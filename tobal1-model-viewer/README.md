# トバルNo.1 モデルビューア

PlayStation 版『トバルNo.1』のディスクイメージを読み込み、中のキャラクターの3Dモデルを
ブラウザで回して見るビューア（非公式）。

**https://haristweet.github.io/tobal1-model-viewer/**

姉妹版の [トバル2 モデルビューア](https://haristweet.github.io/tobal2-model-viewer/) から、
ディスクの読み取り・ゲーム独自の圧縮の展開・モデルの組み立て・描画をそのまま持ってきている。
違うのは **中身の位置を決め打ちしていない** ところ。トバル2 では解析で分かった値
（ファイル表の位置 `0x800CD660`、キャラごとのモデル番号）を直接書いていたが、
No.1 はまだ解析していないので、読み込んだディスクから実行時に探しにいく。

## 使い方

1. 手持ちのトバルNo.1 のディスクイメージをページにドロップするか、「ファイルを選ぶ」で選ぶ
   - `.chd`（MAME の圧縮形式・v5）
   - `.bin`（2352バイト/セクタの生イメージ、または 2048バイトの ISO）
   - ディスクから取り出した実行ファイル（`SLPS_004.00` など）とデータのまとまったファイルの2つ
2. 左の一覧からファイルを選ぶと、その中身をモデルとして組み立てて表示する
3. うまく出ないときは「解析」タブの内容を見る（そのままコピーできる）

ドラッグで回転、右ドラッグ／Shift＋ドラッグで移動、ホイールで拡大、ダブルクリックで視点を戻す。
タッチは1本指で回転、2本指で移動と拡大、ダブルタップで戻す。
背景の色は8種から選べて、「スクショを保存」で PNG（透明背景・キャラだけ切り抜きも可）。

ゲームのデータはこのページに一切含まれていない。読み込んだファイルはブラウザの中だけで読み取り、
どこにも送信しない。

## 中身

`index.html` の1枚のみ。外部ライブラリもビルドも無い（WebGL 直書き）。

### トバル2 から持ってきたもの（そのまま）

- ディスク: ISO9660 の読み取り。CHD はハンクの目次（ハフマン符号）を解いて `cdlz`（LZMA）と
  `cdzl`（Deflate）を展開する。`cdfl`（FLAC）と差分形式には未対応
- 圧縮: ゲーム独自の圧縮（先頭 `0x0b`、4bit モードの256バイト単位ブロック）の展開
- 入れ子: 先頭に「部分の数」と「各部分の位置」が並ぶファイルの分解
- モデル: 表示命令の列（関節選択・頂点変換・ポリゴン命令）をたどって三角形にする
- 骨格: 骨の長さの表と親子関係から T ポーズを組む
- テクスチャ: VRAM 転送列（4bit/8bit 画像とパレット）を仮想 VRAM に置き、パレット引きはシェーダで行う
- BGM: XA-ADPCM（4bit ステレオ 37.8kHz）の展開と Web Audio での再生

### No.1 用に作り直したもの（自動検出）

- **ディスクの中身**: 名前を決め打ちせず、ISO のディレクトリをたどって全ファイルを並べる。
  `SYSTEM.CNF` の `BOOT=` から実行ファイルを決め、音声・動画を除いていちばん大きいファイルを
  アーカイブとみなす
- **ファイル表さがし**: 実行ファイル全体を 8バイト単位で走査し、
  「位置と大きさが隙間なく並んでいるところ」を探す。1件の形は2通り試す
  （`位置 / 大きさ<<8|種類`＝トバル2 と同じ、`位置 / 大きさ`）。表の途中の空きスロット（0埋め）は
  並びを切らさず、末尾に続く 0埋めは表に含めない。候補は「詰まり具合」で並べ、
  「解析」タブから手で切り替えられる。候補の順番は、**その表が指す先を実際に何件か読んで**
  トバルのファイル（入れ子・圧縮）になっているかで決める（詰まり具合だけで決めると、
  たまたま数件そろっただけの短い並びが勝ってしまう）
- **アーカイブさがし**: どのファイルがデータのまとまりかも決め打ちせず、音声・動画
  （`.XA` `.STR` `.DA` など）を除いた大きいファイルを候補として持ち、実際に中身を読んで決める
  （「解析」タブから手でも変えられる）。名前も手がかりにする（`ALLBIN.BIN`、`.BIN` / `.DAT` など。
  `BGM/` `SOUND/` の下は下げる）
- **ファイルの見分け**: 先頭1セクタだけ読んで、入れ子・圧縮・TIM・そのままを見分ける。
  部分0が `0x0b` で始まる入れ子を「モデルらしい」とみなす。種類ごとに数件ずつ調べて一覧に出し、
  「全部のファイルを調べる」で全件まわす
- **テクスチャの部分**: 何番目の部分かを決め打ちせず、VRAM 転送列として筋の通る部分を探す
- **モデルの並びを自分で合わせる**: トバル2 の並び（描画命令が `0x20` から、頂点/面/色の位置が
  ヘッダの語 5/4/7）で組めなかったら、ブロックを見て並びを割り出す。描画命令の開始位置は
  **ヘッダの語が指す先**も候補にし、頂点・面の置き場所は「座標が人の大きさに収まるか」
  「頂点番号が範囲に入るか」「中身が詰まっているか」で選ぶ。三角形が出ても1点に潰れていたら
  当たっていないとみなす
- **骨格**: トバル2 のつなぎ方をそのまま使う。合わないときのために「関節を使わない」
  （モデル内の座標をそのまま出す）に切り替えられる
- **BGM**: XA のセクタのサブヘッダ（チャンネル番号・サブモード）を先頭64セクタだけ読んで、
  何本が何セクタおきに入っているかを数えてから鳴らす
- **テクスチャ（No.1 の形）**: `u32 長さ ; u16 x ; u16 y ; u16 幅 ; u16 高さ ; データ[幅×高さ×2]`
  の繰り返し。`長さ = 12 + 幅×高さ×2` が成り立つ。列の途中に 8 バイトの目印（`0x10, 0x08`）が
  挟まるので、合わない語は 4 バイトずつ読み飛ばして拾い直す
- **モデル本体がどの部分かを探す**: トバル2 は必ず部分0 が本体だったが、No.1 は
  `[8, 8, 85440]` のように**後ろの部分**に入っていることがある。展開できた部分を
  大きい順に試して、三角形が取れたものを採る
- **アーカイブを直接さらう**: ファイル表の形が分からなくても、アーカイブの中を 2048 バイトごとに見て
  「ファイルの先頭らしいところ」（`0x0b` で始まる圧縮、または入れ子の目次）を拾う。
  表からモデルが出てこないときは自動でこちらに切り替わる。さらった結果から、
  実行ファイル内のファイル表の位置と1件の長さを逆算して「解析」に出す
- **解析タブ**: 実行ファイル・アーカイブ・表の候補・種類ごとの件数・選択中のファイルの内訳
  （部分の大きさ、展開後の長さ、描画命令の内訳、三角形の数、骨の数）をテキストで出す

## 状態

**トバルNo.1（SLPS-00400）のディスクで確認中。** ディスクは読めていて、実行ファイル
`SLPS_004.00` と `ALLBIN.BIN`（24.22 MB、トバル2 と同じ名前）を見つけるところまで来ている。
v0.1.3 でアーカイブの選び方を直したところ。

途中、トバル2（`SLPM_860.33`）のイメージでも通しており、**正解と答え合わせができた**おかげで
探索の不具合が3つ見つかっている（下の更新履歴）。

分かっていること（実測）:

- **圧縮形式はトバル2 と同じ。** アーカイブ内のファイルの先頭が `0b 84 de 00` で、
  トバル2 の展開処理のヘッダ（`0x0b` ＋ 展開後の大きさ）として読める
  （圧縮 17,740 バイト → 展開後 56,964 バイト）
- アーカイブの先頭には `dummy` という詰め物が入っている
- **テクスチャファイルの署名**: 先頭16バイトが `語0 = 組数`、`語1 = 0x10`、
  `語2 = 8 か 9`、`語3 = 0x2C（16色）か 0x20C（256色）`。実物 24件で
  「語0 × 2 = 転送枚数」が一致（8→16、20→40、25→50、41→82 …）
- **ファイル表の1件は 20 バイト。** 8バイト刻みで読んだとき、中身のあるエントリが
  `#35, #40, #45, #50 …` と5の倍数に並ぶ（8×5 = 40 = 20×2）。
  さらった結果から逆算した「1件 20 バイト」とも一致する。位置は record の先頭にある
- **音声は `VAGp`（PS1 の VAG 形式）。** 効果音・ボイスが多数
- **面の形**: 12バイトで1枚の三角形。`u32 ×3` が頂点番号で、値は**4倍**で入っている
  （`18 00 00 00 14 00 00 00 10 00 00 00` → 24,20,16 → 頂点 6,5,4）。
  面の領域 768 バイト ÷ 12 = 64枚で、ヘッダの `20+11+33` と一致する
- **モデルの部品の形**（実物 sector 5927 で確認、別の部品でも一致）:
  `面の位置 / 頂点の位置 / 法線の位置 / 色の位置` の4つに続いて、頂点数・法線数・面の数×4種。
  `+0x38` から 頂点（int16 x,y,z,余り）→ 法線 → 面 → 色（u8 r,g,b,余り）が隙間なく並ぶ。
  面は先の2種が8バイト、後の2種が12バイト
- **モデルは `0x90000000` で始まる。** 183件のふるい分けで判明。種類B（`語1 = 0x3C か 0x34`、
  4部分、展開後 30〜43KB、sector 5529〜9289）がキャラクターのモデルらしく、
  種類A（`語1 = 0x14`、109部分、展開後 4KB 前後、sector 6016〜6312）は別のデータ
- **大きいファイルは全部テクスチャだった。** 上位24件（34KB〜214KB）がすべて
  テクスチャの集まり。モデルはもっと小さいところにある
- **テクスチャは読める。** 84KB のファイルから転送 24枚を取り出せた
  （`(416,486) 16×1` パレット → `(624,0) 16×131` 画像 → `(288,504) 16×1` パレット →
  `(678,258) 12×64` 画像 … とパレットと画像が交互）
- **テクスチャの形が分かった。** 84KB のファイル（sector 6408、本体は部分2、展開後 189,680B）は
  モデルではなく**テクスチャの集まり**だった。`u32 長さ ; u16 x,y,幅,高さ ; データ` の繰り返しで、
  1枚目が 16色パレット（`(416,486) 16×1`、BGR555 に最上位ビット）、2枚目が画像
  （`(624,0) 16×131`）。`長さ 0x2C = 12+16×1×2`、`長さ 0x106C = 12+16×131×2` と
  2箇所で検算が合う。本文が `ee ee` `88 88` だらけなのは 4bit 画像だから
- **モデル本体は部分0 とは限らない。** 84KB のファイル（sector 6408）は
  部分の大きさが `[8, 8, 85440]` で、部分0 は展開して 4 バイトしかなく、本体は部分2 だった
- アーカイブの中身は、直接さらった範囲で **183件**（入れ子 54 / 圧縮 15 / 入れ子＋圧縮＝モデル候補 114）
- **モデルの形はトバル2 と違う。描画命令の文法も通用しない**（`0` や `5` で始まり、
  トバル2 の命令表に無い）。展開したブロックの頭が
  `0x90000000 0x00000014 0 0 0 | 0x0000047C 0x0000017C 0x0000047C 0x00000E7C | 5 1 0 6 8 …`。
  先頭に `0x90000000` の印があり、語1（`0x14` ＝ 20）の先に置き場所が4つ並ぶ。
  トバル2 は先頭に印が無く、ヘッダの語 4/5/7 が面/頂点/色を指していた。
  区画は `0x17C`(380) / `0x47C`(1148) / `0xE7C`(3708) / 末尾(4240)。`0x14` からは
  24バイトの繰り返し（`5 1 0 6 8 8` / `5 1 0 12 8 16` …）が並ぶ。
  `0x47C` の中身は `(128, 0, -20)` `(0, 0, -149)` と、頂点らしく読める
- **ファイル表の1件は 8 バイトではない。** 8バイト刻みで読むと、ファイルらしい
  「位置・大きさ」の間に `0x8000EA00` `0x800CC09C` といった RAM のアドレスが挟まる。
  トバル2（1件8バイト = 位置 ＋ 大きさ<<8|種類）とは形が違う

ディスクの中身（実測）:

- `ALLBIN.BIN` 24.22 MB — データのまとまり（トバル2 と同じ名前）
- `SLPS_004.00` 756 KB — 実行ファイル（text=0x80010000、size=0x000BC800）
- `OPENING.STR` / `UDAEND.STR` — 動画
- `BGM/*.DA` 24本 — BGM。CHD では音楽トラックが FLAC（`cdfl`）で圧縮されていて未対応なので、
  いまは鳴らせない（`.XA` ではないため BGM の欄も出ない）

トバル2 は解析済みでファイル表の正解（`0x800CD660` / 1362件）が分かっているため、
このビューアは**トバル2 のディスクを入れると正解と照らし合わせて表示する**ようにしてある
（「解析」タブに「正解のファイル表」と、候補への `✓正解` 印が出る）。
No.1 の前に探し方が正しいかを確かめられる。

作った環境にディスクイメージが無いので、コード自体の確認は合成データで行っている
（ファイル表さがし・候補の並べ方・入れ子・独自圧縮の展開・モデルの組み立て・描画・一覧・
解析タブを、実際にブラウザで動かして確認済み。テスト31件）。

うまく出ないときの見方。

- 一覧は出るがモデルが出ない → 表示命令かテクスチャの形が違う。「解析」の内容が手がかりになる
- ファイル表が見つからない／中身 0% ばかり → 表の形が違うか、アーカイブの選び方が違う。
  「解析」の「大きいファイル 5件」と「表の先頭 8件」を見る
- 「ディスク直読み」に切り替えると、ディスクのファイルを直接見られる

## 未対応

- 動きの再生。トバル2 版は実行ファイルの処理をページ内の MIPS＋GTE エミュレータで
  動かしているが、呼び出すアドレス（`0x80048a40` など）がトバル2 専用なので、
  No.1 の解析ができるまで入れていない
- キャラクターの名前。ファイル番号でしか呼べない（どの番号が誰かは解析待ち）
- 勝利ポーズ・技名・投げの2人表示・動画の書き出し

## 更新履歴（Changelog）

- **v1.0.0** (2026-09-17) — **面の形が分かった。** 面のバイト列を出してみたら、
  `18 00 00 00 | 14 00 00 00 | 10 00 00 00` ＝ 24, 20, 16 と、すべて4の倍数だった。
  4で割ると 6, 5, 4 で頂点番号になる。つまり **面は 12バイトで1枚、`u32 ×3` が頂点番号で、
  値は頂点番号の4倍**（PS1 でよくある、掛け算を省くための持ち方）。
  面の領域 768 バイト ÷ 12 = **64枚ちょうど**で、ヘッダの `20 + 11 + 33 = 64` と一致する。
  `10, 20, 11, 33` の4つを面の数だと思って 8バイト／12バイトに割り振っていたのが間違いで、
  面はすべて 12バイト均一、`10` は別の意味の数字だった。

- **v0.9.3** (2026-09-17) — 実物で 8部品すべて組めて三角形 370枚まで来たが、
  **別々の位置が 60点しかなかった**（8部品で約380頂点あるのに）。つまり面の中の
  頂点番号の読み位置が違う。「頂点番号が範囲に収まる」だけで読み方を選んでいたため、
  同じ頂点ばかり指す読み方でも通ってしまっていた。**使われた頂点の種類数**も点数に入れ、
  面の種類（8バイト×2種・12バイト×2種）ごとに読み方を選ぶようにした。
  あわせて、**面のバイト列を「解析」に出す**ようにした（区画のダンプは先頭と末尾しか
  出しておらず、面は真ん中にあって見えていなかった）。

- **v0.9.2** (2026-09-17) — v0.9.1 でも画面が真っ暗だった（三角形 370・広がり 199・8/9 部品は
  出ていた）。**カメラの半径の求め方が原因**。軸ごとの 2〜98% で枠を決めると、頂点が1か所に
  固まっているとき帯がゼロ幅になり、半径が 1 になって遠くが切り落とされる。
  **中心は各軸の中央値、半径は「中心からの距離の 98% 点」**で決めるようにした。
  つぶれた三角形の法線が出ずに色が壊れるのも直し、解析に「別々の位置が何点あるか」を追加。

- **v0.9.1** (2026-09-17) — v0.9.0 で 8部品すべて組めて三角形が 186 → **370** に増えたが、
  画面には何も出なかった。カメラの枠を頂点の最小・最大で決めていたため、**1点でも飛んだ頂点が
  あるとモデルが点になってしまう**。各軸の 2%〜98% で枠を決めるようにし、
  座標が人の大きさ（±8000）を外れる面は落とすようにした。
  どの部品が変なのか分かるよう、**部品ごとの座標の範囲**を「解析」に出し、
  画面の下の表示にも「広がり」と部品数を出すようにした。

- **v0.9.0** (2026-09-17) — **実物のトバルNo.1 から三角形が出た**（sector 5927 で 186枚）。
  ただし 4/9 部品しか組めていなかった。原因は、頂点数・法線数をヘッダの決まった場所から
  読んでいたこと。実物には**頂点51・法線51 の部品と、頂点47・法線45 の部品が混ざっていた**。
  位置の差から数を割り出すようにし（`頂点数 =（法線の位置 − 頂点の位置）÷ 8`）、
  面の数4種も場所を決め打ちせず「8バイト×2種 ＋ 12バイト×2種 が面の大きさに一致する並び」を
  頭から探すようにした。

- **v0.8.1** (2026-09-17) — v0.8.0 で**モデルを読む処理をページに入れ忘れていた**のを修正
  （`t1ObjectOffsets is not defined`）。テストの束には入れたのに、ページの組み立ての一覧に
  足していなかった。同じ取りこぼしを防ぐため、**組み立てに入っていない `.js` があれば
  ビルドで止める**ようにし、**トバルNo.1 の形のモデルをページ側で通しで組む確認**
  （ブラウザで実際に描いて画素を数える）も追加した。

- **v0.8.0** (2026-09-17) — **トバルNo.1 のモデルの形が分かったので、専用の読み方を実装した。**
  実物（sector 5927、展開後 42,744B）のダンプから、部品ひとつの形が読めた。

  ```
  +0x00 面の位置   +0x04 頂点の位置   +0x08 法線の位置   +0x0C 色の位置
  +0x1C 頂点の数   +0x20 法線の数     +0x24 面の数×4種   +0x34 0
  +0x38 頂点 int16 x,y,z,余り → 法線 → 面 → 色 u8 r,g,b,余り
  ```

  面は先の2種が8バイト、後の2種が12バイト。`0x6F10` の部品（51頂点、面 10/20/11/33）で
  `10×8 + 20×8 + 11×12 + 33×12 = 768` が色の位置とぴったり合い、`0x7644` の部品
  （48頂点）でも一致した。部品の位置はファイルの先頭に「個数 ＋ その数だけの位置」の組で
  並び、0 で終わる。トバル2 の並び（面/頂点/?/色）と同じで、16バイトぶん短いだけだった。

- **v0.7.1** (2026-09-17) — v0.7.0 で**ふるいをかける順番を間違えていた**のを修正。
  当てにならないファイル表の一覧（523件）にかけてしまい、でたらめな大きさで
  確保に失敗して（`Array buffer allocation failed` 277件）モデルが開かれなかった。
  アーカイブを直接さらった結果に切り替えてからふるいをかけるようにし、
  読み込む大きさに上限（16MB）を設けた。あわせて **`VAGp`（PS1 の音声）**も
  見分けるようにした（効果音・ボイスが多数あった）。

- **v0.7.0** (2026-09-17) — **モデルのファイルを特定した。** 183件をふるいにかけたら、
  `0x90000000` で始まるファイル群が出てきた。2種類ある。
  - 種類B: `90000000 | 3C か 34 | 04 | 位置4つ | 04`。4部分・展開後 30〜43KB。
    sector 5529, 5544, 5737, 5927, 5943, 8755, 9289 — **キャラクターのモデルらしい**
  - 種類A: `90000000 | 14 | 0 0 0 | 位置4つ …`。109部分・展開後 4KB 前後。
    sector 6016〜6312 — モーションかクエストのデータらしい

  この署名を見分けて、**署名のあるファイルを優先して開く**ようにした（テクスチャは飛ばす）。
  読み込み時に必ず全件ふるいにかけ、署名のある候補を「解析」に一覧で出す。

- **v0.6.0** (2026-09-17) — **テクスチャファイルの署名が分かった**ので、候補 114件を
  全部ふるいにかけるようにした。大きい 24件が全部テクスチャだったため、
  モデルはもっと小さいところにあるはず。署名は先頭16バイトで判る:
  `語0 = テクスチャの組数（パレット＋画像。転送はこの2倍）`、`語1 = 0x10`、
  `語2 = 8 か 9`、`語3 = 最初のパレットの長さ（0x2C = 16色 / 0x20C = 256色）`。
  実物 24件で「語0 × 2 = 転送枚数」が一致した。署名があるファイルは組み立てずに弾き、
  残ったものだけをモデル候補として先頭24バイトと語0〜7 を出す。
  組数から求まる枚数がそろうまで転送を拾い続けるようにもした
  （`[8, 8, 大]` の形のファイルで取りこぼしていた）。

- **v0.5.1** (2026-09-17) — テクスチャ読みが実物で動いた（84KB のファイルから**転送 24枚**。
  `(416,486) 16×1` のパレットと `(624,0) 16×131` の画像が交互に並ぶ）。それを踏まえて、
  **テクスチャと分かったファイルはモデル候補から外し**、次の候補を探すようにした（16件まで）。
  どれも組めなかったときは**「モデル候補をまとめて調べる」を自動で走らせて**結果を「解析」に出す
  （ボタンを押さなくてよい）。表に展開した中身の先頭16バイトと「まだ分からないもの」の件数を追加。

- **v0.5.0** (2026-09-17) — **トバルNo.1 のテクスチャを読めるようにした。**
  実物の 84KB のファイルを展開したら、モデルではなくテクスチャの集まりだった。
  形は `u32 長さ ; u16 x ; u16 y ; u16 幅 ; u16 高さ ; データ[幅×高さ×2]` の繰り返しで、
  `長さ = 12 + 幅×高さ×2`。列の途中に 8 バイトの目印が挟まるので読み飛ばして拾い直す。
  テクスチャだと分かったファイルは「テクスチャの集まりのようです（転送 N枚）」と表示し、
  モデル探しを続けられるようにした。「まとめて調べる」の表にもテクスチャの枚数を出す。
  実物のバイト列そのものを使ったテストを追加（パレット `(416,486) 16×1`、
  画像 `(624,0) 16×131` を読めること）。テストは 62 → 68 件。

- **v0.4.0** (2026-09-17) — **モデル本体がどの部分に入っているかを探す**ようにした。
  実物の 84KB のファイルは部分の大きさが `[8, 8, 85440]` で、部分0 は展開して 4 バイト
  しかなく、本体は部分2 だった。トバル2 のつもりで部分0 を決め打ちしていたため、
  4 バイトのブロックのヘッダを読もうとして落ちていた（`Offset is outside the bounds of
  the DataView`）。展開できた部分を大きい順に試し、三角形が取れたものを採る。
  短いブロックでヘッダを読んでも落ちないようにし、「まとめて調べる」の表にも
  どの部分が本体だったかを出すようにした。

- **v0.3.2** (2026-09-17) — **モデル候補をまとめて調べる**を追加（「解析」タブのボタン）。
  候補を大きい順に40件開き、大きさ・部分の数・展開後の大きさ・**先頭の印**・三角形が取れたかを
  一覧にする。形が1種類なのか複数あるのか、どれがキャラ本体なのかを一度に見分けるため。
  あわせて、**解析のダンプ作りで落ちていたのを修正**（ヘッダの語がブロックの終わり際を
  指していると範囲外を読んでいた／実物の #108 で発生）。ダンプ作りで失敗しても組み立ては続け、
  途中で落ちてもそこまでの手がかりを解析に出すようにした。

- **v0.3.1** (2026-09-17) — 解析のダンプを、ヘッダが示す**区画ごと**に出すようにした
  （区画の範囲と大きさ、先頭 96 バイトと末尾 32 バイト、`+0024` からの 24バイトごとの並び）。
  トバルNo.1 の描画命令はトバル2 の文法が通用しないことが分かったため、
  形を決めるにはもう少しデータが要る。あわせて、モデル候補は**大きいものから**試すようにした
  （キャラクターのモデルは小物より大きいはず）。

- **v0.3.0** (2026-09-17) — **モデルの並びを自分で合わせる**ようにした。実物のトバルNo.1 の
  モデルを展開したら、頭が `0x90000000 0x14 … 0x47C 0x17C 0x47C 0xE7C` という形で、
  トバル2（ヘッダの語 4/5/7 が面/頂点/色）とはまったく違う並びだった。
  - 描画命令の開始位置の候補に、**ヘッダの語が指す先**を足した（固定値だけ試していたので届いていなかった）
  - 頂点・面の置き場所は「座標が人の大きさに収まるか」「頂点番号が範囲に入るか」
    「中身が詰まっているか」で点数を付けて選ぶ
  - 三角形が出ても**1点に潰れていたら外れ**とみなして組み直す（ゴミを表示しないため）
  - 合わなかったときのために、展開した中身の頭 128 バイトとヘッダの語が指す先を「解析」に出す

- **v0.2.1** (2026-09-17) — 直接さらったあと、**「圧縮」だけのファイルをモデルとして開いていた**のを修正。
  モデル候補は「入れ子で部分0が圧縮」のものだけにし、1件目が組めなければ次の候補を順に試す
  （最大8件）。三角形が取れなかったときも例外にせず、**ブロックの頭（32bit×16）と
  0x20 からの生の描画命令**を「解析」に出すようにした。形が違うときの手がかりになる。

- **v0.2.0** (2026-09-17) — **ファイル表に頼らずアーカイブを直接さらう**モードを追加。
  トバルNo.1 のファイル表は1件が8バイトではなく（8バイト刻みで読むと RAM のアドレスが
  挟まる）、形がまだ分からないため、表を当てにいくのをやめてアーカイブの中を
  2048 バイトごとに見て、ファイルの先頭（`0x0b` の圧縮、または入れ子の目次）を拾うようにした。
  表からモデルが出てこないときは自動でこちらに切り替わる。
  あわせて、さらった結果から実行ファイル内のファイル表の位置・1件の長さ・大きさの位置を
  逆算して「解析」に出す（次に表を読むための手がかり）。
  `0x0b` で始まるだけの偶然を拾わないよう、展開後の大きさと最初のブロックのモードまで見る。

- **v0.1.3** (2026-09-17) — トバルNo.1（SLPS-00400）の実物で見つかった不具合を修正。
  - **アーカイブに `BGM/SABAKU.DA` を選んでいた。** BGM が `BGM/*.DA` に入っていて、
    `ALLBIN.BIN`（24.22 MB）より大きいものが6本あるため、大きさ順の候補が全部 BGM で
    埋まって `ALLBIN.BIN` に届いていなかった。しかも CHD の音楽トラックは FLAC 圧縮で
    読もうとすると例外になるため、ファイルの先頭がすべて「読めず」になっていた。
    `.DA` などを音声として除き、名前もアーカイブらしさの手がかりに加えた
  - どのアーカイブでも 0% だったとき、最後に試したものではなく**いちばんマシだったもの**を残すように
  - 1件の大きさの下限を 8 → 64 バイトに。「sector 11 / 大きさ 10」のような並びが
    候補の上位に来ていたのを防ぐ
  - BGM さがしで例外が出ても読み込み自体は続けるように

- **v0.1.2** (2026-09-17) — トバル2 のディスク（正解が分かっている）に当てて見つかった不具合を修正。
  - **入れ子の終わりの見方**が厳しすぎた。ファイルの大きさはセクタの切りで余りが付くことがあるのに
    `部分の終わり === 大きさ` を要求していたため、本物の入れ子ファイルが全部「そのまま」に落ち、
    候補の中身がすべて 0% になっていた（`<=` に修正）
  - **セクタが順番に並んでいることを必須にしていた**ため、順番が飛ぶたびに並びが切れ、
    1362件の表が 40件・33件・138件…と6つ以上に割れていた。連続は「質」として数えるだけにし、
    空きスロットや変な1件は8件まで飛ばすようにした
  - 候補の並べ方を「中身の割合」から「確かめられた実ファイルの本数（割合×件数）」に変更。
    アーカイブに素のデータが混ざっていても短い誤検出に負けない
  - アーカイブも候補から選べるように（全部 0% のときは自動で次の候補を試す）
  - 解析済みのディスク（トバル2）を入れたら気づいて知らせ、正解と照らし合わせる
  - 1ファイルの上限を 8MB → 32MB に

- **v0.1.1** (2026-09-17) — 実物のディスクで1回通して見つかった不具合を修正。
  ファイル表の候補を「詰まり率」で並べていたため、たまたま数件そろっただけの短い並びが
  本物より上に来ていた。**候補の指す先を実際に読んで中身を確かめてから並べる**ようにした
  （一覧に「中身 ○%」として出る）。あわせて、窓が狭いときに背景の丸と右上の操作、
  下の状態表示と操作のヒントが重なっていたのを修正。解析タブに「表の先頭 8件」と
  「大きいファイル 5件」を追加。
- **v0.1.0** (2026-09-17) — 最初の版。トバル2 版から器を移植し、ファイル表・モデル・
  テクスチャ・XA のチャンネルを自動で探すようにした。実物での確認はこれから。

## v1.1.0 — 本体の部品の正体を探る

v1.0.0 で面の形（1枚 12 バイト、u32×3、値は頂点番号の4倍）が確定し、
腕・脚らしい 8 部品はどれも「64枚の面が全部有効・頂点を1つ残らず使う」という
きれいな読み方になった。残るのは先頭の部品 `+0x0034` で、

- 頂点 854・法線 470 と、他の部品（頂点 50前後）よりひとまわり大きい
- 面の領域が 12,736 バイト = 12 × 1061 + 4 で、12 で割り切れない
- ヘッダ（+0x38 まで）と頂点のあいだに 1,504 バイトの空きがある

の3点が他と違う。これが混ざったとたん広がりが 199 → 724 に跳ねるので、
画面が人の形に見えない原因はここだと見ている。

この版でやったこと:

- 面の読み方に「頭を数バイト飛ばす」を足した。割り切れない部品でも、
  余りのぶんだけ頭をずらせば筋の通る読み方になるかを試す。
- 部品ヘッダの先頭 14 個の u32 を生のまま報告に出す。
  `+0x10`〜`+0x18` が部品の置き場所なのかどうかを実物の数字で確かめるため。
- ヘッダと頂点のあいだの空き（1,504 バイト）を 16 バイトずつ並べて出す。
  骨の並びか、部品の入れ子の目録だと思われる。
- 部品ごとの三角形の数と座標の範囲を報告に出す。
  部品がそれぞれ別の場所にあるのか、全部が原点に重なっているのかが分かる。

## v1.2.0 — 面の本当の形が分かった（法線つき）

v1.1.0 で出した「面の中身」のダンプで、1枚の面の形が完全に決まった。

```
0c 00 00 00  08 00 00 00  04 00 00 00  00 00 00 00   頂点番号 u32 ×4（どれも4倍）
25 02 63 f8  e8 0d 00 00                             法線 int16 x,y,z,0
```

- 頂点番号は `12, 8, 4, 0` → 4で割って `3, 2, 1, 0`
- 続く8バイトは `(549, -1949, 3560, 0)`。長さを測ると **4095.6 ≒ 4096**

4096 は 1.12 の固定小数の 1.0 なので、これは**単位法線**。
ダンプに出ていた4枚ぶんを全部測ったら 4095.6 / 4095.0 / 4095.4 / 4094.9 と
どれも 4096 だったので、読み違いではない。

    三角形 = 頂点番号 u32 ×3 ＋ 法線 8バイト = 20 バイト
    四角形 = 頂点番号 u32 ×4 ＋ 法線 8バイト = 24 バイト

この2つが混ざって並ぶ。本体の面の領域 12,736 バイトが 12 でも 24 でも
割り切れなかったのはこのため。1枚ずつ「法線がどこに来るか」で見分ける。

### もうひとつ分かったこと: 8つの部品はキャラクターではない

`+0x6F10` から並ぶ8部品は、この読み方では通らない。法線を持たず、
頂点番号3つ12バイトがずらりと並ぶだけ（どれもきっかり64枚）。
形も x が 3 から始まって左右対称の2組4本。**当たり判定か影のための形**で、
画面に出すものではないと判断した。

**キャラクターの見た目は `+0x0034` の部品ひとつ**（頂点854・法線470）だった。
v1.0.0 で「広がりが 199 → 724 に跳ねる」と書いたのは、
当たり判定と見た目を一緒くたに出していたせい。

この版でやったこと:

- 法線つきの面の読み方を実装（三角20バイト・四角24バイトの混在を1枚ずつ判定）
- ファイルに入っている法線をそのまま陰影に使う（面の裏表の取り違えがなくなる）
- 法線を持たない部品はふだん出さない。「当たり判定も出す」で切り替えられる
- 実物のバイト列をそのまま使った試験を5つ追加。
  仕掛け用のモデルにも法線つきの部品を足して、通しの確認でも読み方が効くようにした

## v1.3.0 — 面の並びがどこで途切れるかを見る

v1.2.0 の読み方は、実物では通らなかった（9部品とも「法線なし」のまま）。
ただし v1.2.0 の報告に、答えに近い数字が出ていた。

    面の中身（部品 +0x0034、頂点854）… 尻 90 00 00 00 cc 00 00 00 f0 00 00 00 8c 00 00 00
    → 頂点番号 36, 51, 60, 35

頂点が854個ある部品なのに、**いちばん後ろの面が 36〜60 番を指している**。
つまり頂点番号は部品全体の通し番号ではなく、**中の小部品ごとに 0 から数え直している**。
本体は小部品（頭・胴・腕…）の入れ物で、面の並びのあいだに区切りが挟まっている
――と考えると、v1.2.0 の読み方が途中で止まるのも筋が通る。

この版は、その区切りの正体を見るための計測だけを足した:

- 途切れても最後まで歩き、「四角/三角の数・読めた割合・途切れの数・かたまりの数・
  いちばん大きい頂点番号」を報告に出す
- **途切れた場所の前後 48 バイトを生で出す**。区切りに何が入っているかを直接見る
- 読めた割合が 75% を超えていればキャラクターの見た目とみなす（それまでは
  「途切れゼロ」を求めていたので、入れ子のある本体が弾かれていた）

## v1.4.0 — 面のうしろの「おまけ」と、番号を共有する法線

v1.3.0 でも画面は変わらなかった（＝法線つきの読み方はまだ通っていない）。
報告を待つあいだに、すでに見えている数字から2つ直した。

### 1. 8部品の法線は、頂点と同じ番号で引く

8部品の色を見ると `ff c1 a3`（255,193,163）＝肌の色だった。
当たり判定ではなく、ちゃんと目に見える形だということ。
そして 8部品はどれも**頂点の数と法線の数が同じ**（51/51, 48/48, 47/47）。
本体だけが違う（854/470）。

    頂点の数 ＝ 法線の数 の部品 → 面は頂点番号3つだけ。法線は同じ番号で引く（なめらか）
    頂点の数 ≠ 法線の数 の部品 → 面のうしろに法線が1つ入っている（平ら）

PS1 のモデルでよくある2通りの持ち方そのままだった。
v1.3.0 までは 8部品の法線をまったく使わず、三角形の向きから計算していた。

### 2. 面のうしろに余分が付く形に対応

テクスチャの貼り方（UV や色パレットの位置）が面のうしろに付いていると、
1枚の長さが 24 バイトより長くなって読み方が途中で止まる。
1枚読むごとに、次の1枚がちゃんと始まる位置まで 4 バイト単位で読み飛ばし、
**読み飛ばした長さの内訳を報告に出す**ようにした。
ここに 8B や 12B がずらりと並べば、それがテクスチャのぶんということになる。

## v1.5.0 — 面は4種類あった

v1.4.0 の報告で、途切れた場所の前後がそのまま答えだった。

```
… 53 01 10 f0 6e 00 00 00 ┃ 0c 00 00 00 08 00 00 00 04 00 00 00 00 00 00 00 …
```

`┃` の左は法線 `(339, -4080, 110)`（長さ 4095.5 ＝ 4096）で終わっている。
右は `12, 8, 4, 0` → 4で割って `3, 2, 1, 0`、続けて `2, 5, 4, 1` `7, 6, 3…` と
**法線を持たない四角形**が並ぶ。辺を共有していく、いわゆる帯の並び方。

つまり面は4種類ある。PS1 のモデルでよくある、平らな面となめらかな面の組み合わせ。

| 種類 | 形 | 法線 | 1枚の長さ |
|---|---|---|---|
| A | 三角 | なし（頂点の法線を使う） | 12 バイト |
| B | 四角 | なし | 16 バイト |
| C | 三角 | あり | 20 バイト |
| D | 四角 | あり | 24 バイト |

本体 `+0x0034` は 70% が C/D、残り 30% が A/B だった。

### どこで切り替わるかは書かれていない

ファイルのどこにも「ここから法線なし」とは書かれていないので、読む側で決めるしかない。
しかも 12バイトの三角2枚と 16バイトの四角1枚は、どちらでもバイト数が埋まってしまう。

そこで2段構えにした。

1. **ぜんぶ同じ形で隙間なく並ぶか**をまず見る。腕や脚の8部品は 12バイトの三角64枚で
   768 バイトちょうど埋まるので、これで確定する。
2. 埋まらないとき（本体）は、**いちばん多くのバイトを説明できる並べ方**を
   後ろから決めていく。同点のときは「形として筋が通っているか」で選ぶ:
   - 同じ頂点が混ざっていないか
   - 四角なら4点が平らに乗っているか
   - 持っている法線が、実際の3点から出る面の向きと合っているか（これがいちばん強い手掛かり）

v1.4.0 では本体の 30%（3,832バイト）を捨てていた。そのぶんが画面の突き出た破片の正体。

## v1.6.0 — 頂点番号が途中で 0 に戻っている疑い

v1.5.0 で面は 1,094 → 1,544 枚に増えて、ほぼ全部読めるようになった。
それでも画面は細長い破片が放射状に突き出た形のまま。これは
**頂点番号の基準がずれている**ときの典型的な見え方で、心当たりがある:

    本体 +0x0034 は頂点が 854 個あるのに、面が使う番号の最大は 276 しかない

番号が部品全体の通し番号なら、854個のうち後半 3分の2 が一度も使われないことになる。
そんなモデルは作らない。つまり番号は**小部品ごとに 0 から振り直されて**いて、
小部品の頭の位置を足さないと本来の頂点に届かない。

この版でやったこと:

- 面を並び順に見て、**大きい番号を使ったあと急に 0 付近へ戻る場所**で区切る
- 区切りごとの「番号の幅」を順に足していき、**頂点の数と合うか**を確かめる。
  合えば番号の振り直しは確定なので、**その場で足し込んで正しい頂点を指すようにする**
- 合わないときは何もしない（当て推量で形を壊さない）
- ヘッダと頂点のあいだの 1,504 バイトを、376個の32bit として全部報告に出す。
  小部品の目録ならここにあるはず

## v1.7.0 — 区切りは計算では決まらない。バイトを見る

v1.6.0 の狙い（区切りの幅を足して頂点の数と合えば確定）は外れた。
36個に区切れて合計 2,833、頂点の数 854 とは合わない。

そこで「区切りの中では 0 から最大まで頂点をひとつ残らず使う」「幅の合計が頂点の数」の
2つを同時に満たす切り方を、当てずっぽうではなく総当たりで解く仕組みを書いた。
結果は**条件を満たす切り方が何通りも成り立ってしまう**というもので、
正解を一つに絞れない。作りものの筒のモデルで試しても、正しい切り方とは別の
「条件は満たすが間違った切り方」を返す。この道は行き止まり。

推測を重ねても埋まらないので、計測に戻る。この版で足したもの:

- **頂点を64個ずつ区切って置き場所の範囲を出す**。小部品ごとに固まっているなら、
  区切りの位置で範囲がはっきり飛ぶ。飛ばなければ「小部品ごとに振り直し」という
  見立て自体が間違っている
- **面の並びの切れ目まわりの生バイト**（前24バイト・後32バイト）。
  区切りの目印が挟まっているなら、v1.4.0 のときと同じくここに出る
- ヘッダと頂点のあいだの 1,504 バイトを 376個の32bit として全部出す

### 1,504 バイトについて分かったこと

全部出してみたら、**108個ぶんの並びがそっくりそのまま2回**現れた。
さらに別の 30個ぶんも2回。左右対称の体を持つキャラクターらしい形をしている。
`5` のあとは必ず 1 か 2 が来て、`-4` の前は必ず 6。
`5 1` `5 2` で切ると 33個に分かれ、最後の1個は

    5, 2, 6, 50, 48, 10, 21, 11, 36, 1, 0, 63, 8, 47, 9, 30, 0

で、腕や脚の部品のヘッダ `5, 2, 0, 51, 51, 10, 20, 11, 33, 0` と形がよく似ている
（頂点の数・法線の数・面の数4種）。ただし 33個の小部品に 854頂点なら平均26頂点で、
面が使う番号が 0..167 に届くことと合わない。まだ読み切れていない。

## v1.8.0 — ゲーム本体のコードを読む（MIPS 逆アセンブラ）

エミュレータ（DuckStation）でメモリを見てもらったところ、2つ分かった。

**1. ゲームはポインタを書き換えていない。**
メモリ上のモデルも `面 0x24B8 / 頂点 0x5C0 / 法線 0x1FE8 / 色 0x61EC` と、
**ファイル内の相対位置のまま**だった。実アドレスに直していない。
つまりファイルの中身＝メモリの中身で、読み方の土台は合っている。

**2. `5, 1, X` の X は 0 とは限らない。**
別のキャラクターでは `5, 1, 46` で、`-4` の代わりに `-20` が出ていた。
どちらも4の倍数の負の数。命令の列らしさが強まった。

データはもう足りている。足りないのは**それをどう解釈するコード**のほう。
そしてそれは手元にある — 実行ファイル SLPS_004.00 はビューアがすでに読んでいる。

    ゲームは必ず署名 0x90000000 と比べる
      → コードに `lui ○○, 0x9000` が必ずある
      → バイト列で 00 90 ?? 3C
      → そこがモデルを読む関数

そこでビューアに **MIPS(R3000) の逆アセンブラ**を積んだ。

- `lui ??, 0x9000` を実行ファイル全体から探す
- 見つけた場所から前後へ、関数の切れ目（`addiu $sp,$sp,-N` ／ `jr $ra`）まで広げる
- その関数をまるごと逆アセンブルして報告に出す
- GTE（座標変換の専用回路）の命令も読める。頂点を触るところに必ず出てくるので、
  `RTPT` や `MVMVA` が見えればそこが変換の本体

これで「番号 0..167 をどうやって 854個の頂点に届かせているか」が、
推測ではなくコードで確かめられる。

## v1.9.0 — ファイルの頭の形が、コードで確定した

v1.8.0 の逆アセンブラが `0x80068080` の関数を当てた。これがモデルのファイルを開く本体。
読み解くと、ファイルの頭はこうなっている（**推測ではなくコードそのもの**）:

```
lw   $s0, 2796($s2)      ptr = ファイルの先頭
lw   $v1, 0($s0)         署名を読む
bne  $v1, $v0, 抜ける    0x90000000 でなければ終わり
addu $s4, $s0, $zero     base = ファイルの先頭          ★控える
addiu $s0, $s0, 4
lw   $a0, 0($s0)         offset0 を読む
addu $a0, $s4, $a0       base + offset0                 ★足す
jal  0x8001BE24          ← 部品を読む関数へ
...
lhu  $v0, 0($s0)         個数（16ビット）
loop:
  lw   $a0, 0($s0)       部品の位置
  addu $a0, $s4, $a0     base + 位置                    ★また足す
  jal  0x8001BE24
```

分かったこと:

- **位置はすべてファイルの先頭からの相対**。`base + 位置` で実アドレスになる。
  ビューアの読み方と一致していた
- **個数は `lhu`（16ビット）で読んでいる**。32ビットだと思っていたが、
  上位16ビットは別の意味かもしれない
- 部品の列は**3組**ある（#68 は 4個 / 4個 / 0個）。
  「0 で終わり」だと思っていたのは「3組目が0個」だった
- **部品を読む本体は `0x8001BE24`**。頂点の番号をどう扱うかはこの関数の中

この版では、見つけた関数が `jal` で呼んでいる先も自動で辿って逆アセンブルする。
`jal` の飛び先が負の数で出ていた不具合（符号なしにし忘れ）も直した。

## v2.0.0 — 1,504バイトの正体は「命令の列」だった

呼び出し先 `0x8001EEE4` が全部教えてくれた。

```
lw $s2, 0($fp)        面の位置
lw $a0, 4($fp)        頂点の位置
lw $s3, 8($fp)        法線の位置
lw $s4, 12($fp)       色の位置
addiu $fp, $fp, 16    ← ヘッダは16バイトだけ。この先は命令の列

ループ:
  lw $v1, 0($fp)      命令コード
  lw $s6, 4($fp)      引数
  lh $t1, 1014($s0)   いまの「ベース」（16ビット、スクラッチパッド 0x1F8003F6）
  addu $t1, $s6, $t1  ベース ＋ 引数
  addiu $fp, $fp, 8   命令は8バイト
  jr $v1
  sh $t1, 1014($s0)   ★ ベース = ベース + 引数（累積）
```

**部品のヘッダは4つの位置（16バイト）だけ**で、そのあとは
**8バイトずつの「命令コード＋引数」の列**だった。

そして `0x1F8003F6`（スクラッチパッド）に**16ビットの「ベース」**を持ち、
命令のたびに引数を足し込んでいる。**これが 854対167 の答え**。
頂点番号は部品の通し番号ではなく、**このベースからの相対**だった。
「小部品ごとに振り直し」という見立ては当たっていたが、区切りの幅は
面から推測するものではなく、**命令列に書いてあった**。

裏も取れた。腕の部品 `+0x6F10` を命令として読むと:

| 位置 | 中身 | 意味 |
|---|---|---|
| +0x10 | `(5, 2)` | 命令5、引数2 |
| +0x18〜+0x30 | `0, 51, 51, 10, 20, 11, 33` | 命令5が読むデータ（頂点51・法線51・面の数） |
| +0x34 | `0` | **命令0 ＝ 終了** |

v1.0.0 から「部品は 0 で終わる」と書いてきたが、正体は**終了命令**だった。

命令の振り分けも読めた:

    命令 1 → 0x8001F1B0    命令 2 → 0x8001F264    命令 3 → 0x8001F428
    命令 4 → 0x800205DC    命令 5 → 0x8001F44C    命令 6 → 0x8001F434
    命令 7 → 0x8001F1E0    命令 8以上 → 飛び先の表（0x80021FCC など）

この版では、逆アセンブラが**分岐の飛び先**と**飛び先の表**も自動で読むようにした。
命令ごとに何をするか（どれだけデータを食うか、ベースをどう動かすか）が分かれば、
モデルの読み方はそのままコードに書き写せる。

## v2.1.0 — 「頂点番号 ×4」の正体が分かった

命令1の処理（`0x8001F1B0`）が決定打だった。

```
sll  $v0, $s6, 1        v0 = 引数 × 2
lw   $a3, 8($fp)        3つ目の語を読む（命令1は8バイトではなく12バイト）
lw   $a0, 1016($s0)     頂点ポインタ（スクラッチパッド 0x1F8003F8）
sll  $t0, $a3, 3        3つ目の語 × 8   ← 頂点は8バイト
addu $t0, $a0, $t0
sw   $t0, 1016($s0)     ★ 頂点ポインタを「その個数ぶん」進める
subu $a2, $s1, $v0
sll  $v0, $v0, 1        引数 × 4
jal  0x8001F024         ← RTPT（座標変換）へ
addu $a1, $s0, $v0      ★ 置き場所 = 0x1F800000 + 引数×4
j    ループへ
addiu $fp, $fp, 12      命令1は 12 バイト
```

つまり命令1は **「頂点を N 個読んで座標変換し、スロット番号 M から順に置く」**。

    命令1 = (1, 置き場所M, 個数N)   12バイト
      → 頂点ポインタから N 個読む（頂点ポインタは N×8 進む）
      → 変換した結果を スクラッチパッドの M×4 の位置から順に置く

そして **面の「頂点番号 ×4」は、このスロットの位置そのもの**だった。
スクラッチパッドは 1KB（0x1F800000〜0x1F8003FF）しかないので、
**スロットは最大でも 250個ほど**。実測で番号の最大が 167 だったのは当然だった。

頂点854個に対して番号が 0..167 なのは「振り直し」ですらなく、
**頂点はいったん小さな置き場所に移され、面はその置き場所を指している**という仕組み。
命令列を頭から実行して「どのスロットに何番の頂点が入っているか」を追えば、
面が本当はどの頂点を指しているかが分かる。

この版では、**命令ごとの「1つの長さ」をコードから自動で割り出す**ようにした。
どの処理も最後は `j ループの先頭` に戻り、その遅延スロットで `addiu $fp, $fp, N`
と進めている。その N を全命令ぶん拾って一覧にする。
あわせて「頂点ポインタを動かすか」「何語目まで読むか」「座標変換するか」も出す。
これが揃えば、あとは同じ手順をなぞるだけでモデルが組める。

## v3.0.0 — 形式が完全に解けた

v2.1.0 が出した「命令ごとの長さ」の一覧で、最後の鍵が手に入った。
**命令5 は 4 バイト**。これで全部が噛み合った。

腕の部品 `+0x6F10` を命令として読むと:

| 位置 | 中身 | 意味 |
|---|---|---|
| +0x10 | `5` | 命令5（4バイト） |
| +0x14 | `2, 0, 51, 51` | 命令2（16バイト）頂点51・法線51を置き場所0から |
| +0x24 | `10, 20` | 命令10（8バイト）**三角を20枚** |
| +0x2C | `11, 33` | 命令11（8バイト）**四角を33枚** |
| +0x34 | `0` | 終了 |

v1.0.0 からずっと「面の数4種 10/20/11/33」だと思っていた数字は、
**数ではなく `命令10 引数20` と `命令11 引数33`** だった。
そして 20×12 ＋ 33×16 = **768 バイト**で、面の領域とぴったり合う。

### 本体でも完全に一致

本体 `+0x0034` の命令列（386語）をそのまま実行すると:

- 命令 163個で **+0x614 で終了** — 頂点データが始まる直前ちょうど
- 読んだ頂点は **854個** — ファイルの頂点数と完全一致
- 面は 666枚で、1枚の大きさの組み合わせが**一意に** 12,736 バイトに一致

      命令8: 193枚 × 20B（三角＋法線）
      命令9: 210枚 × 24B（四角＋法線）
      命令10: 93枚 × 12B（三角）
      命令11: 170枚 × 16B（四角）
      合計 12,736 バイト ✓

### 形式のまとめ（推測なし・すべてコードと実データで裏付け済み）

```
部品の頭16バイト   面/頂点/法線/色 の位置（部品の先頭からの相対）
+0x10 から        命令コード(u32) ＋ 引数(u32) の列

  0        終了
  1  12B   (1, 置き場所, 個数)         頂点を個数ぶん読み、置き場所から順に入れる
  2  16B   (2, 置き場所, 個数, 個数2)  頂点と法線を読む
  3   8B / 4 16B / 5 4B / 6 8B / 7 12B   行列や描画の設定（形には関わらない）
  8以上 8B (命令, 枚数)                面を枚数ぶん描く

面の1枚:  8 → 20B 三角＋法線 / 9 → 24B 四角＋法線 / 10 → 12B 三角 / 11 → 16B 四角
面の頂点番号は「置き場所 × 4」。本当の頂点番号ではない
```

**「頂点854個なのに番号が 0..167 まで」の答え**: 置き場所はスクラッチパッド1KBぶん
しかなく、命令1/2 が頂点をそこへ小分けに運び込む。面はその置き場所を指している。
命令列を順に実行して「どの置き場所に何番の頂点が入ったか」を追えば、
面が本当はどの頂点を指しているかが確定する。

この版から、ビューアは**部品を命令の列として実行して**モデルを組む。
読んだ頂点の数がファイルの頂点数と合わなければ、これまでの推測の読み方に落ちる。

## v3.1.0 — 人の形になった

v3.0.0 で実物のキャラクターが表示された。顔・フード・V字の襟・脚まで見て取れる。
報告の数字も完全に一致:

```
+0x6F10  命令4個　頂点51個　面53枚（命令10:20枚 命令11:33枚）
+0x0034  命令163個　頂点854個　面666枚
         （命令8:193枚 命令9:210枚 命令10:93枚 命令11:170枚）
```

9部品すべてが「命令で実行」で読め、頂点数もファイルの値と完全一致している。
推測で当てている部分はもう無い。

この版は仕上げ:

- カメラの合わせ枠を 98% → 99.9% に。読み方が固まって飛んだ頂点が出なくなったので、
  ほぼ全部が画面に入るようにした（v0.9 以降ずっと寄りすぎていた）
- 形式が解けたので、途中で使っていた計測をすべて畳んだ。
  頂点番号の区切り探し、頂点の置き場所の窓、面の切れ目のダンプ、
  ヘッダと頂点のあいだの生ダンプ、飛び先の表、命令ごとの逆アセンブル — どれも役目を終えた。
  報告に残すのは「命令の一覧」と、部品ごとの実行結果だけ
- 「当たり判定も出す」→「読めない部品も出す」。8部品は当たり判定ではなく
  ちゃんとした見た目の一部だったので、名前を直した

### 残っていること

- テクスチャ（29枚見つかっているが、まだモデルに貼っていない）
- キャラクターごとのファイルの対応（どの sector が誰か）
- 命令 3/4/6/7 の意味（形には関わらないが、骨やポーズに関係しそう）

## v3.2.0 — 「人の形になった」は早とちりだった

v3.1.0 を「解けた」と書いたのは間違い。正面から見るとそれらしく見えるが、
**回すと長い棘が放射状に出ている**。人の形ではない。

### 何が確かで、何が駄目か

確かなこと（これは偶然では起きない）:

- 命令163個で頂点データの直前ぴったりに終わる
- 読んだ頂点が854個 ＝ ファイルの値と完全一致
- 面のバイト数が 12,736 ＝ 領域と完全一致

この3つが独立に一致しているので、**命令の読み方そのものは合っている**。

駄目なこと: 長い棘。これは「離れた2点をつないだ三角形」で、
**部品がそれぞれの原点に重なって置かれている**ときの見え方。

### 心当たり

命令5 の処理（`0x8001F44C`）は、`0x800DBE90` / `0x800DBE8C` の配列から
**行列を1つ取り出して GTE に積む**。つまり:

    命令5 ＝ 次の骨の行列を使う

部品は骨ごとに別々の座標系にあり、行列で正しい位置に運ぶ必要がある。
それを全部まとめて原点に置いているのが今の状態。正面がそれらしく見えたのは偶然。

裏付けになりそうな数字もある。8部品の座標は x が **3 から** 始まって
120 / 144 / 187 / 204 まで伸びる。これは「関節から骨に沿って伸びる」形そのもの。

### この版で足したもの

- 命令5 で区切って、**区切りごとの座標の範囲と中心**を報告に出す

区切りがそれぞれこぢんまり固まっていて中心が近ければ、
「部品は別々の座標系にあり、行列で運ばないといけない」で確定する。
区切りの中でも棘が出ているなら、置き場所の対応がまだどこか間違っている。

## v3.3.0 — 切り分けの道具（本体だけ／骨ごとに色分け）

v3.2.0 の「骨ごとの置き場所」で、**行列で運べば直る**という見立ては外れた。
区切りが小さくまとまっていない（広がり 630 / 465 / 399 …）。

ただし、同じ数字が別のことを教えてくれた。

```
[ 6] 24枚  範囲 x  -4..395  y  -74..62   z -57..70     X方向に長く、Y/Zは細い
[ 7] 14枚  範囲 x -70..395  y -125..37   z -41..43     同じ
[ 3] 230枚 範囲 x -336..294 y -201..164  z -208..209   大きな塊
```

**X方向に長い細い部品**が並んでいる。腕や脚の形そのもの。
つまり**モデルはX軸に沿って寝ている**可能性が高い。

さらに左右対称のペアがきれいに出ている:

    [4]↔[10]  [5]↔[11]  [6]↔[12]  [7]↔[13]  [8]↔[14]  [9]↔[15]
    [17]↔[23] [18]↔[24] [19]↔[25] [20]↔[26]      ← z だけ符号が反転

骨の構造として完全に筋が通っている。読み方が大きく外れているなら、
こんなにきれいな左右対称は出ない。

### この版で足したもの

どこがおかしいのかを目で切り分けるための道具:

- **本体だけ** — いちばん頂点の多い部品（キャラクターの胴体）だけを出す。
  これだけで人に見えるなら、残り8部品の置き場所の問題に絞れる
- **骨ごとに色分け** — 命令5 の区切りごとに色を変える。
  棘がどの骨から出ているかが目で追える
- モデル全体の縦横（X/Y/Zそれぞれの長さ）を報告に出す。
  いちばん長い軸が背の高さのはず

## v3.4.0 — 背の高さの軸は X だった

v3.3.0 で出した1行が決定的だった。

```
モデル全体の縦横: X 811　Y 411　Z 417
```

**X : Y : Z ＝ 2 : 1 : 1。** 人の縦横比そのもの（身長は肩幅の約2倍）。
つまり**モデルの「背の高さ」は X 軸**で、ビューアは Y を上として描いていたので、
**キャラクターは寝ていた**。

これで v3.1.0 の「回すと棘だらけ」も説明がつく。寝たキャラクターを
足元や頭上から覗き込めば、胴体は輪切りの円筒に見え、マントの縁は棘に見える。

骨ごとの範囲を並べ直すと、X が背の高さだという読みと合う:

```
[ 6] 24枚  範囲 x  -4..395  y -74..62   z -57..70    X に長く Y/Z が細い ＝ 手足
[ 7] 14枚  範囲 x -70..395  y -125..37  z -41..43    同じ
[20]  6枚  範囲 x   0..62   y -46..48   z -41..36    小さくまとまった部品
```

さらに左右対称のペアが10組（[4]↔[10] [5]↔[11] [6]↔[12] [7]↔[13] [8]↔[14]
[9]↔[15] [17]↔[23] [18]↔[24] [19]↔[25] [20]↔[26]、z だけ符号反転）。
読み方が大きく外れていれば、こんなにきれいな対称は出ない。

### この版で足したもの

**向き**の切り替え（左の欄、「そのまま / X を上に / X を上（逆） / Z を上に」）。
どの軸が背の高さかはファイルに書かれていないので、選べるようにした。
**初期値は「X を上に」**。

頂点も法線も同じ回し方で付け替えるので、陰影も一緒に正しく回る。

## v3.5.0 — 上下をひっくり返した

v3.4.0 で「X を上に」を初期値にしたところ、確かに背の高さは合ったが**天地が逆**だった
（足が上、頭が下）。プレステの座標は Y が下向きに増える流儀で、このモデルでは
背の高さの軸である X も足のほうが大きい。

そこで**初期値を「X を上（逆）」に変更**した（`(x,y,z) → (y,-x,z)`）。
向きの記憶（localStorage）も作り直したので、前の版で覚えた上下逆の向きは引き継がれない。

### 上下が合っているかを数で見る

見た目の印象だけで判断すると間違えるので、解析結果に**横幅の断面**を足した。
背の高さの軸を10段に切って、それぞれの段の横幅（X と Z の広いほう）を並べる:

```
  上から下への横幅: 頭 → 肩 → 腰 → 足 の順
```

人の形なら、上から2〜3段目（肩と腕）がいちばん広く、いちばん上（頭）は細い。
逆さまなら広い段が下に寄る。角度を変えて見るまでもなく、この数列で判定できる。

## v3.6.0 — 部品がどこに置かれるのかを追う

v3.5.0 で上下は直ったが、まだ人には見えない。読み直して分かったことと、
そのために足した道具。

### ファイルは「本体1つ＋4つ組が2つ」

先頭の表をもう一度読むと、部品は3つの組に分かれている:

```
offset0        → +0x0034   本体（頂点854個・面666枚）
組1（4個）     → +0x6F10 +0x7644 +0x7D3C +0x8420
組2（4個）     → +0x8B04 +0x9238 +0x9930 +0xA014
```

組1と組2の4個は、面の数が全部そろって53枚（命令10で三角20枚・命令11で四角33枚）、
座標は z の符号だけが逆。**左右対になった4つずつの部品**で、
x は 3..120 / 3..144 / 3..187 / 3..204 と少しずつ長くなる。
どれも x=3 から始まる＝**それぞれ自分の原点から生えている**ということで、
行列で運ばないと全部が同じ場所に積み重なる。今そうなっていて、
画面の棘はこの8個が原点に固まったもの。

本体のほうは、命令5で区切った26区切りのうち10組が z 反転の対になっていて、
こちらは最初から左右に開いた1つの座標系に収まっている。

### この版で足したもの

**「部品を並べる」**（画面上のチェック）。9個の部品を横一列に離して置く。
重なったままだと1つ1つが何の形なのか分からないので、
腕なのか布なのか棘なのかを目で確かめるための道具。

**解析結果に命令 3/4/5/6/7 の中身**を出すようにした。
長さは分かっている（3→8B、4→16B、5→4B、6→8B、7→12B）が、
何をする命令なのかはまだ分からない。置き場所（行列）の正体はこの中にあるはず。

```
命令  3 → 0x8001F428   8B
命令  6 → 0x8001F434   8B
命令  5 → 0x8001F44C   4B
```

3つが 36 バイトの中に固まっている＝どれも数命令の小さな処理で、
行列の積み下ろしのような役目だと思われる。

## v3.7.0 — 8個の部品の正体は「手」だった

「部品を並べる」で横に離して見たら、はっきり分かった。
**組1と組2の8個は、指まで作り込まれた手**。4つずつが**同じ手のポーズ違い**で、
左右で1組ずつ。だから面の数が53枚でそろっていて、z の符号だけが逆だった。

つまりこの8個は**同時に出るものではない**。ゲームは技に応じて組から1つ選ぶ。
全部出していたから原点で重なって棘になっていた。

そこで、**組の中身の面の大きさがすべて同じなら「ポーズ違い」と見なして
組から1つだけ出す**ようにした。面の大きさがばらばらな組は別々の部品なので
そのまま全部出す（トバル2 由来の試験データがこれ）。

### 命令 3/5/6 が読めた

実行ファイルの中身を見たら、3つとも数命令の短いものだった。

```
命令3 (8B)  $s3 += 引数            ← データを読む位置をずらす
命令6 (8B)  旗が立っていなければ $s7 += 引数
命令5 (4B)  0x800CBE90 / 0x800CBE8C にある「表」から次の1件を取り出す
```

命令5 がいちばん大事で、こうなっている:

```
t0 = *(0x800CBE90)        表を指すポインタを2つ読む
t1 = *(0x800CBE8C)
t0 += 4 ; t1 += 4         1件ぶん進めて
*(0x800CBE90) = t0        進めた位置を書き戻す
t5 = 前の t0 の中身        取り出した1件は「行列を指すポインタ」
t0 = *(t5) ; t1 = *(t5+4)  そこから行列を読む
```

**命令5＝「次の骨に進む」**。部品の置き場所（行列）はモデルファイルの中ではなく、
外から渡される表にある。そして 0x8001BE24 は、0x800CC910 が指す **32バイトの塊**を
コピーして +32 する＝**行列を積む**処理だった。プレステの MATRIX は
3×3 の int16（18バイト＋詰め物）＋ int32 3つの移動量でちょうど32バイトなので、
これで間違いない。

### この版で足したもの

- 解析結果に、**まだ読めていない部分（部分1・部分2・部分3）の中身**を出すようにした。
  ファイル #68 は [29076, 1648, 8, 988] の4部分で、読めているのは部分0 だけ。
  骨の表がこのどれかにある見込みが高い。1648 バイトという大きさは、
  他のキャラクターでも 1630〜1680 とほぼ同じで、骨の本数ぶんの表に見える。
- 0x800CBE90 / 0x800CBE8C / 0x800CC910 を**書き込んでいる場所**を実行ファイル全体から
  探して、その前後を出すようにした（`mipsRefs`）。表を用意しているのが誰かが分かれば、
  骨にたどり着く。

## v3.8.0 — 1つのファイルに 0x90000000 のモデルが3つ入っていた

骨の表を探して部分1・部分3 を開いてみたら、**どちらも 0x90000000 のモデル**だった。
骨ではなかった。

```
部分0  29076 B → 42744 B   部品9個・頂点854個   ← キャラクター本体
部分1   1648 B →  3144 B   頂点110個・法線なし  ← 部品の少ない簡易版
部分2      8 B →     4 B   中身は 0
部分3    988 B →  1892 B   頂点74個・法線なし   ← さらに少ない簡易版
```

部分1・部分3 は**法線の位置が面の位置と同じ＝法線を持たない**作りで、
これまでの読み方（頂点＜法線＜面＜色 を厳しく見ていた）では途中で投げ出していた。
等号を許すようにして、読めるようにした。

頂点が 854 → 110 → 74 と落ちていくので、遠景用の簡易版だと思われる。

**ファイルの中のモデル**という選択欄を足したので、どれを出すか選べる。

### 骨はモデルファイルの中にはない

命令5 が引く表は外から渡されるもので、モデルファイルの4つの部分のどこにもない。
アーカイブの中で、キャラクターのモデル（sector 5927）のすぐ後ろにある
sector 6016〜6312 のファイル群は、`928` バイトの塊が何十個も並んだ形をしている:

```
# 97〜#102  [24/752/752/3468/1416/428/4412/928/928/928/928/…]
```

同じ大きさの塊が並ぶ＝技ごと・フレームごとのデータに見える。骨はこちらにありそう。

## v3.9.0 — 骨さがし

命令5 が引く表は「行列を指すポインタの並び」だった。その行列はプレステの MATRIX で、
**ちょうど32バイト**:

```
int16 m[9]   回転（1.3.12 の固定小数。1.0 が 4096）  18 B
詰め物                                                2 B
int32 t[3]   移動量                                   12 B
                                                 計 32 B
```

キャラクターのモデル（sector 5927）のすぐ後ろ、sector 6016〜6312 のファイルには
**928 バイトの塊**が何十個も並んでいる。そして

```
928 ÷ 32 = 29  ← ちょうど割り切れる
```

本体の命令5 の区切りが26個、それに手が左右で2個、根っこが1個で 29。数が合う。

### 見分け方

でたらめなバイト列を行列と読み違えないように、**回転行列であることを確かめる**:

- 3本の行の長さがどれも 4096（±12%）
- 3本が互いに直角（内積が 0.12 未満）

この2つを同時に満たすバイト列は、でたらめでは出てこない。試験でも、
本物と同じ作りで作った29個は全部通り、でたらめな並びは1個も通らなかった。

### 使い方

左の欄の**「骨さがし（32バイトの行列を探す）」**を押すと、アーカイブの中の
183ファイルを全部あたって、32バイトの行列が並んでいる塊を探す。
見つかったものは解析結果に、回転と移動量の中身つきで出る。

## v3.10.0 — 骨さがしを自動で走らせる

v3.9.0 のボタンは押さないと動かないので、**ファイルをふるいにかける処理に組み込んだ**。
起動して一覧を出せば、骨さがしの結果が解析結果にそのまま出る。

### 持ち方を3通り試す

行列の持ち方が1つとは限らないので、ありそうな形を全部あたる:

```
32 B  回転 int16×9 ＋ 詰め物 ＋ 移動 int32×3   ← プレステの MATRIX そのもの
24 B  回転 int16×9 ＋ 移動 int16×3
20 B  回転 int16×9 だけ
```

さらに、塊の頭に小さな見出しが付いている見込みも考えて、
**頭を 0 / 4 / 8 / 16 バイト飛ばした読み方**も試す。
いちばん筋が通った組み合わせを採る。

判定はこれまでどおり「3本の行の長さがどれも 4096（±12%）で、互いに直角」。
試験では 32バイトの持ち方も 24バイトの持ち方も正しく見分け、
でたらめな並びは1個も通らなかった（試験145件すべて通過）。

## v3.11.0 — 「見つからない」から穴を2つ見つけた

骨さがしは **見つかりませんでした** に終わった。3通りの持ち方 × 4通りの頭飛ばしを
全部試して1件も出ないのはおかしいので、探し方のほうを疑い直したら、穴が2つあった。

### 穴1: 番地を追う手が足りていなかった

0x800CBE90 を「使っている場所」が、命令5 の中の**読み1・書き1 の2件だけ**だった。
誰も表を用意していないことになり、ありえない。理由は探し方にあった:

```
lui  $at, 0x800d
sw   $t0, -16752($at)     ← これは拾えていた
```

```
lui   $at, 0x800d
addiu $v0, $at, -16756     ← いったん番地を作って
sw    $t1, 4($v0)          ← そこを土台に書く（構造体ごし）
```

**後者を追えていなかった**。`addiu` と `ori` で番地を作る形を追うようにした。
これで構造体ごしの書き込みも見つかる。

### 穴2: 目盛りを 4096 に決め打ちしていた

回転行列かどうかの判定で「3本の行の長さが 4096」を条件にしていた。
けれど 1.0 をいくつで表すかは作り手の勝手で、4096 とは限らない。

**目盛りを決め打ちしないように直した**。「3本の長さが互いにそろっていて、互いに直角」
なら、目盛りがいくつでも回転行列。見つかったときは 1.0 がいくつだったかも出す。
試験では 1.0＝16384 の行列もちゃんと見分けた。

### 足したもの

**モデルを描く関数（0x80068080）の中身**を丸ごと解析結果に出すようにした。
骨の表を用意しているのはこの関数のはずなので、ここを読めば答えが出る。

## v3.12.0 — モデルを描く関数が読めた

0x80068080 が読めて、ファイルの読み方がコードから裏づけられた。

```
s2 = 相手（キャラクターの構造体）
s0 = *(s2+0xAEC)          モデルの先頭
*(s2+0xAE4..0xAE8) = 0    3つの数を0に
if(*s0 != 0x90000000) → 単純に描いて終わり
s4 = s0                   ← 以後の位置はすべてこの先頭からの相対
a0 = s4 + *(s0+4)         offset0 の部品
jal 0x8001BE24            描く
*(s2+0xAEC) = a0

count1 = 半語 *(s0)       ※半語で読むが、位置は4バイト進む
*(s2+0xAE4) = count1
count1回:
  a0 = s4 + *(s0)         組1の部品
  *(s3+0xB40) = a0        ← 覚えておく場所。4バイトずつ
  jal 0x8001BE24          描く
count2 → *(s3+0xB50)
count3 → *(s3+0xB60)
```

### 分かったこと3つ

**1. ファイルの読み方は合っていた。** 数は半語で読むが位置は4バイト進む、という
作りまで含めて、これまでの読み方と一致する。

**2. 組は「4個まで」と決まっている。** 覚えておく場所が 0xB40 / 0xB50 / 0xB60 と
16バイト刻み＝ポインタ4つぶん。組の大きさが4なのは偶然ではなかった。

**3. ゲームは組の部品を全部描いている。** ループの中で毎回 0x8001BE24 を呼んでいる。
つまり「組から1つだけ」というこの版の見せ方は、**ゲームの動きとは違う**。
ただし置き場所（行列）が分からないうちは全部が原点に重なって棘になるだけなので、
表示の都合としてはこのままにしておく。

### 0x800CBE88 / 8C / 90 は3つ並んだ構造体

0x8001BE24 は描く前に 0x800CBE88 に 1 を、終わったら 0 を書く。
この番地は 0x800CBE8C の4バイト手前で、3つが並んでいる:

```
0x800CBE88  旗（描いている間だけ 1）
0x800CBE8C  表2 を指すポインタ
0x800CBE90  表1 を指すポインタ
```

番地を1つずつ探していると、**構造体の先頭を土台にして別のずれで書く形**を
取りこぼす。そこで `mipsRefsRange` を足して、0x800CBE80〜0x800CBE9F の
**範囲まるごと**を読み書きしている場所を探すようにした。

あわせて、実際に部品を読み進める関数 **0x8001BBE0**（0x8001BE24 が呼んでいる先）
の中身も出すようにした。骨の表を用意しているのはこのあたりのはず。

## v3.13.0 — 表は3本あった。そして命令の表がもう1つあった

範囲で探したら 18件出た。そこに**3本目の表**があった。

```
0x8001bb44  lui   $v1, 0x800d
0x8001bb48  lw    $v1, -16748($v1)   v1 = *(0x800CBE94)
0x8001bb50  addiu $v0, $v1, 4
0x8001bb58  sw    $v0, -16748($at)   *(0x800CBE94) = v1 + 4   ← 1件ぶん進める
0x8001bb5c  lw    $a0, 0($v1)        取り出した1件は部品のポインタ
0x8001bb60  jal   0x8001bbe0         その部品を読み進める
```

命令5 とまったく同じ「表から1件取って進める」形。並びはこうなっている:

```
0x800CBE88  旗（描いている間だけ 1）
0x800CBE8C  表B
0x800CBE90  表A          ← 命令5 が引く
0x800CBE94  表C（部品のポインタ）← 0x8001BB40 が引く
```

**どの表も「進める」だけで、誰も用意していない。** 18件すべてが読みか、自分で進めた
書き戻し。つまり用意する側は、**番地を register に作って関数に渡している**はず。
読み書きの命令としては現れないので、`mipsAddrOf` を足した ——
lui＋addiu で番地を組み立てているだけの場所を探す。

### 部品の頭の形がコードで確定した

0x8001BBE0 の先頭が、部品の頭をそのまま読んでいた:

```
v0 = *(部品+0)  → 面ポインタ    gp+0xD8C に保存
（+4 の頂点ポインタはここでは読まない）
v0 = *(部品+8)  → 法線ポインタ  gp+0xE98
v0 = *(部品+12) → 色ポインタ    gp+0xD74
部品+16 から命令の列
```

**+0 面 / +4 頂点 / +8 法線 / +12 色 / +16 命令** ——
これまでの読み方とコードが完全に一致した。

### 命令の表がもう1つあった

0x8001BBE0 は自前の命令ループを持っていて、**0x80010308 に15件のジャンプ表**がある。

```
v1 = 命令 - 1
if(v1 >= 15) 打ち切り        ← 命令は 1..15
v0 = *(0x80010308 + v1*4)
jr v0
```

0x8001EEE4 のもの（0x80021FCC）とは**別の表**。
つまり**解釈器が2つある**。こちらも中身を出すようにした。

あわせて、旗が立っていないときに呼ばれる 0x80019C78（下ごしらえの見込み）も出す。

## v3.14.0 — メモリの写しを読む

骨の表は**実行時にしか存在しない**。実行ファイルをいくら静的に追っても、
3本の表（0x800CBE8C / 90 / 94）はどれも「進める」だけで、誰が用意しているのか出てこない。
用意する側は番地を register に作って渡しているので、静的解析では一番きついパターン。

そこで、**プレステのメモリの写しを直接読む**ようにした。
DuckStation でゲームを動かしてセーブステートを作れば、その中に RAM 2MB がそのまま入る。

### 形式は決め打ちしない

セーブステートの形式を解析するのではなく、**実行ファイルの中身そのものを手がかりに
RAM の位置を探す**。ディスクから読んだ SLPS_004.00 の本体の先頭64バイトが、
写しの中のどこにあるかを探せば、そこが RAM の 0x10000 にあたる。

```
写しの中で exe[0x800..0x840] が見つかった位置 − 0x10000 ＝ RAM の先頭
```

この探し方なら、生のメモリ書き出しでもセーブステートでも、
**RAM が圧縮されずに入ってさえいれば**形式を問わず読める。

### 読むもの

```
0x800CBE88  旗
0x800CBE8C  表B
0x800CBE90  表A ← 命令5 が引く
0x800CBE94  表C（部品）
0x800CC910  行列積みの現在地
```

表は「進めたあと」の位置を指しているので、**手前 0x40 バイトから32件**並べて出す
（今の位置は括弧で囲む）。さらに表の先を辿って、その32バイトが回転行列として
読めるかまで確かめる。

試験では、見出し付きの偽セーブステートを作って RAM の先頭を正しく当て、
表の先の29個の行列を「回転として筋が通る」と判定できることを確かめた。

## v3.15.0 — 圧縮されたセーブステートに対応する

v3.14.0 でセーブステートを読ませたら「実行ファイルが見つかりませんでした」。
DuckStation はセーブステートを圧縮して書くので、中身がそのままでは見えない。

### やったこと3つ

**1. ほどく。** ブラウザが自前でほどけるのは deflate / gzip なので、
ファイルの中から zlib や gzip の印を探し、そこからほどいて、
実行ファイルが現れるかを確かめる。途中で切れても、
1MB ぶん取れていればそこまでを使う。

**2. zstd なら正直に言う。** DuckStation の今どきの既定は zstd で、
ブラウザはこれをほどけない。印を見つけたらその旨と、
確実な代わりの手（メモリをそのまま書き出す）を出す。

**3. 手がかりを増やす。** 実行ファイルの先頭は起動後に書き換わっていることがある。
そこで手がかりを1か所ではなく、+0x800 / +0x10800 / +0x20800 / +0x40800 / +0x60800 の
5か所で試すようにした。どの手がかりで当たっても、RAM の先頭は正しく逆算できる。

### 見つからなかったときに何を出すか

前の版は「見つかりませんでした」としか言わなかった。これでは次の手が打てないので、
**ファイルの大きさ・先頭64バイト・読める字・見つかった圧縮の印**を出すようにした。
形式が分かれば対応できる。

試験では、作りものの写しで「手がかりが消えたときに、でたらめな場所を答えない」ことも
確かめた（繰り返しのある中身だと、ずれた場所に当たってしまうため）。

## v3.16.0 — 形式が確定した。DuckStation の設定ひとつ

読めなかったファイルの先頭がこうだった:

```
44 55 43 43  57 00 00 00  e3 83 88 e3 83 90 e3 83 ab …
D  U  C  C   版 0x57      「トバルナンバーワン」（UTF-8）
```

**DuckStation のセーブステート（版 0x57）**で確定。そして決め手はファイルの大きさで、

```
1,540,998 B ＜ 2,097,152 B（プレステの RAM）
```

**RAM より小さい＝中身は確実に圧縮されている。** 推測ではなく、大きさから断定できる。
見つかった印も zstd で、ブラウザはこれをほどけない。

### 直し方は設定ひとつ

DuckStation の 設定 → 詳細（Advanced）に「セーブステートの圧縮」がある。

```
Deflate / zlib      → このページがほどける（おすすめ）
なし / Uncompressed → 2MB を超えるのでそのまま読める
zstd（既定）        → ブラウザがほどけない
```

### この版で足したもの

- **"DUCC" の見出しを読む。** 版と題名を出し、ファイルが RAM より小さければ
  「確実に圧縮されている」と断定して言う。あいまいな言い方をやめた
- **ほどく試行を広げた。** zlib/gzip の印を 6 か所までしか試していなかったのを
  16 か所に、探す範囲も 0x40000 から 2MB に広げた
- **直し方を具体的に出す。** 「圧縮を切ってください」では探せないので、
  設定の場所と選ぶ項目を名指しで出すようにした

## v3.17.0 — テクスチャ：貼り先が無いことが数で確定した

テクスチャを貼ろうとして、まず面のバイトを1枚ぶんずつ数えた。

```
命令8  三角＋法線 20B ＝ 頂点番号 12 ＋ 法線 8  → 余り 0
命令9  四角＋法線 24B ＝ 頂点番号 16 ＋ 法線 8  → 余り 0
命令10 三角      12B ＝ 頂点番号 12           → 余り 0
命令11 四角      16B ＝ 頂点番号 16           → 余り 0

193×20 ＋ 210×24 ＋ 93×12 ＋ 170×16 ＝ 12,736 ＝ 面の領域ぴったり
```

**1バイトも余らない。** UV（貼り先）の入る場所が物理的に無い。
部品の頭も 面／頂点／法線／色 の4つだけで、テクスチャを指す場所が無い。
色は頂点ごとの RGB（`d6 d4 d4` の灰、`ff c1 a3` の肌色…）。

つまり**トバルNo.1 のキャラクターは、テクスチャではなく頂点の色で塗られている**。
これは推測ではなく、バイト数の勘定から出た結論。
「テクスチャを貼る」は、やりようが無いのではなく **貼る対象が無い**。

### では 29 個のテクスチャは何か

背景・顔・文字など、キャラクター以外のものに使われている。せっかく読めるので、
**中身をそのまま絵にして並べる道具**を足した（左の欄「テクスチャ」）。

VRAM への転送は「パレット1枚 ＋ 画像1枚」の組で並んでいる。
パレットは高さ1で幅16（16色）か幅256（256色）なので、そこで見分ける。
16色なら16bitの語に4画素、256色なら2画素入るので、**絵の幅は転送の幅の4倍か2倍**になる。
色は BGR555 で、全部0 の色は透ける。

試験では、実物と同じ形の小さなテクスチャを手で組み立てて、
幅が4倍になること・0番が透けること・赤緑青が正しく出ることを確かめた。

### 解析結果に出るもの

```
  テクスチャの貼り先: なし　面の 12736 バイトを頂点番号と法線で使い切っている
```

判定の理由まで出すので、他のキャラクターで違う結果が出れば、それも分かる。

## v3.18.0 — テクスチャの正体は「顔」だった

#118（sector 6811）を絵にしたら、こうだった:

```
88×121  目（黒目あり）        ┐
88×121  目（閉じている）      │ 4枚＝まばたき・表情の差し替え
88×121  目（黒目あり・逆向き）│
88×121  目（閉じている・逆）  ┘
80×53   口（閉じ）            ┐ 2枚
80×53   口（開き）            ┘
36×52   赤い三角（印か記号）
16×16   縞模様
```

**キャラクターの顔のパーツ**だった。v3.17.0 の結論は正しいが、言い方が足りなかったので直す:

- **体**は頂点の色だけで塗られている（貼り先が1バイトも無い、で確定）
- **顔**は目と口を別に、テクスチャを貼った板として描いている

だから体のモデルに UV が無いのは当たり前で、顔は別の仕組みで描かれている。
目が4枚あるのは、まばたきと表情を差し替えるため。

### この版で足したもの

テクスチャの選択に **「すべて並べて見る」** を追加した。29件ぜんぶを順に開いて、
各ファイルの先頭3枚を番号つきで並べる。同じ顔ぶれ（目・口）が並ぶので、
**どのファイルが誰の顔なのか**を見比べられる。

これは次の課題「キャラクターとファイルの対応」に直結する。
顔が分かればキャラクターが分かり、モデルのファイルとも結びつけられる。

## v3.19.0 — キャラクターを見分ける

「すべて並べて見る」で29件を見たら、テクスチャの大半は**クエストモードの背景**だった:

```
#107 #108   扉・取っ手・PUSH ELEVATOR の札
#110        EPISODE 1/2/3　Level ★★
#160 #165 #179 #180   壁・鎖・天井の金具
#166 #174 #176 #177   風景（砂丘・山・白い塔）・惑星
#121        トバルNo.1 のロゴ、クロノ・トリガーの告知
#163        チェッカー模様の看板
#118        目4枚・口2枚 ← 顔はここだけ
```

**顔は #118 だけ。** 目と口の絵は全キャラクターで共有していて、
体の色でキャラクターを描き分けているらしい。

### 残るのは「どのファイルが誰か」

これは絵を見比べるのが一番早い。そこで **「モデルを並べて見る」** を足した。

署名つきで「語1 が 0x14 でない」ファイル＝キャラクターのモデルを大きい順に32体まで、
順に描いて小さな絵にして並べる。描いた絵から**キャラクターのいる所だけを切り出して**
正方形にそろえるので、大きさの違う相手どうしでも見比べやすい。
絵を押すと、そのモデルに切り替わる。

### 動くことを機械で確かめている

並べた絵が真っ黒（描けていない）では意味がないので、通しの試験で
**canvas の中身を読んで、不透明な画素が200個以上ある絵が1枚以上ある**ことまで
確かめるようにした。見た目の確認を人の目に頼らない。

## v3.20.0 — どのモデルが読めていないかを絵に出す

32体を並べたら、2つのことが一度に見えた。

### 1. 色違いが並んでいる

```
#29 sector 5559  白
#30 sector 5572  黄
#31 sector 5585  水色
#32 sector 5598  緑
#33              紫
#34              赤
```

**同じ形の色違いが6体。** sector も 13 ずつきれいに並んでいる。
キャラクターの色替えか、クエストの敵の色違い。
形で見分けるときは、この6体をひとまとまりとして扱えばいい。

### 2. 形が崩れているモデルが混じっている

#45 #124 #127 #135 #136 #148 #149 などは、色の破片が散らばった絵になる。
きちんと立って見える #68 #60 #69 #143 とは明らかに違う。

原因は**部品を命令の列として実行できず、当てずっぽうの読み方に落ちている**こと。
これまでは、それが絵を見ただけでは分からなかった。

そこで一覧の見出しに**三角形の数と読み方の内訳**を出すようにした:

```
#68　sector 5927
三角形 1218　命令9          ← 9部品すべて命令の列として実行できた
#45　sector 5737
三角形 980　命令3／推測5     ← 5部品が読み切れていない（橙色で目立たせる）
```

解析結果にも同じものを出す:

```
  部品の読み方: 命令の列として実行 9個（全部きちんと読めています）
```

**どのモデルが読めていないかが分かれば、次に直すべき場所が決まる。**
これまで #68 ばかり見ていたので、ほかのモデルで何が起きているのかが見えていなかった。

## v3.21.0 — 一覧を1枚の画像で保存できる

モデルの一覧もテクスチャの一覧も、画面をスクロールしながら何度もスクリーンショットを
撮るしかなかった。**「この一覧を画像で保存」**を両方に足した。

並べた絵を見出しごと1枚の PNG にまとめる:

- 1行6枚で並べ、行数は中身に合わせて伸びる
- 透けている所が分かるように市松模様を敷く
- 見出し（`#68 sector 5927` / `三角形 1218 命令9`）を絵の下に入れる
- スマホでは共有シートが開く（`saveBlob` が端末に合わせて振り分ける）

### 保存できていることを機械で確かめる

「ボタンを押したら保存される」を目で確かめるのは当てにならないので、通しの試験で
**保存の出口を一時的に差し替えて、出てきたものが本物の PNG かどうか**を調べるようにした。

```
先頭8バイトが 137 80 78 71 13 10 26 10（PNG の印）
大きさが 2000 バイトを超えている
```

試験では 12,818 バイトの PNG ができることを確認した。

## v3.22.0 — 「命令0」は嘘だった（数える場所が早すぎた）

一覧を1枚にして見たら、**32体すべてが「命令0」**と出ていた。
#68 は9部品すべて命令の列として実行できているはずなので、これはおかしい。

原因は数える場所だった:

```js
info.t1Parts = …                    // ここで数えていた
{ for(const o of okObjs) if(o.run) run++ }   // ← o.run はまだ決まっていない
…
const r1 = buildT1Mesh(b.out, okObjs);       // ← ここで o.run が決まる
```

**読み方を決めるのは `buildT1Mesh`**。その前に数えていたので、`o.run` は
どの部品でも `undefined`、つまり全部 0 になっていた。
当てずっぽうの数も 0 なので、橙色の目印も一度も出ていなかった。

数える場所を `buildT1Mesh` のあとに移した。

### 同じ間違いを繰り返さないように

これは「動いているように見えて、出ている数字が全部嘘」という種類の間違いで、
絵を見ても気づけない。通しの試験に、**見出しと解析結果の両方に 1 以上の数が
出ていること**を確かめる項目を足した。

```
読み方の内訳: ok（部品の読み方: 命令の列として実行 3個／当てずっぽう 1個）
```

直す前のコードならこの試験は落ちる。

## v3.23.0 — 読めない理由を言わせる

数字が正しくなったら、相関がはっきり出た。

```
命令9        #68 #60 #69           → きちんと立っている3体
命令1／推測2  #29〜#37（色違い9体）  → 3部品のうち1つしか読めていない
命令0／推測3〜5  それ以外           → 全部ぐちゃぐちゃ
```

**読めた部品の数と、絵の崩れ具合がぴったり対応している。** 見立ては合っていた。

### 「読めない」だけでは直せない

これまで `t1Run` は読めないとき `null` を返すだけだった。
どの命令で、どこで、なぜ諦めたのかが残らないので、直しようがない。

そこで**諦めた理由をその場で書き残す**ようにした:

```
知らない命令 99（+0x10）
命令2 の置き場所 300／個数 80 が枠(256)に収まらない（+0x24）
まだ入れていない置き場所 41 を指している（+0x368）
頂点番号 18 が4の倍数でない（+0x370）
面の並びが色の手前を越えた（+0x5e8）
読んだ頂点 512個 とファイルの頂点 854個 が合わない
```

位置も一緒に出すので、そのバイトを直接見に行ける。

### 一覧を作ると、32体ぶんの理由がまとめて出る

「モデルを並べて見る」を押すと、読めなかったモデルの理由が解析結果に並ぶ:

```
一覧で読めなかったモデルと、その理由:
  #45 sector 5737　命令0／推測4　知らない命令 …
  #124 sector 8770　命令0／推測4　…
```

**1回の操作で、29体ぶんの原因が一度に分かる。** 1体ずつ調べる必要がない。

試験でも、知らない命令に当たったときとおかしな置き場所を指したときに、
ちゃんとその理由が残ることを確かめている。

## v3.24.0 — 読めない理由を、ボタンを押さずに出す

v3.23.0 で理由を言わせるようにしたが、**「モデルを並べて見る」を押さないと出ない**
作りだった。報告を見たら、その欄がまるごと無かった。
押してもらう前提の道具は、押されなければ無いのと同じ。

そこで**ふるい分けの処理に組み込んだ**。起動して一覧を出せば、
キャラクターのモデル全件の内訳が解析結果に自動で出る。

`t1Diagnose` は絵にせず「読めるかどうか」だけを確かめるので速い。
183ファイルを展開するついでに通せる。

### 理由を種類ごとにまとめる

29体ぶんの理由をそのまま並べても読めないので、
**位置と数を伏せて「種類」でまとめる**ようにした:

```
まだ入れていない置き場所 41 を指している（+0x368）
まだ入れていない置き場所  7 を指している（+0x12）
  ↓ どちらも
まだ入れていない置き場所 N を指している
```

出てくる形はこうなる:

```
  ── キャラクターのモデル 32件: 全部読めた 3件 / 読めないものがある 29件 ──
    24件: まだ入れていない置き場所 N を指している
      例: まだ入れていない置き場所 41 を指している（+0x368）　（#45 #124 #127 …）
     5件: 知らない命令 N
      例: 知らない命令 12（+0x1a4）　（#29 #30 …）
    #45 sector 5737　部品4個中 読めた0個
    …
```

**理由が何種類あるか**が一目で分かる。1〜2種類に集中していれば、
そこを直すだけで29体まとめて読めるようになる。

## v3.25.0 — 命令は15まであった

自動で出るようにしたら、37体ぶんの理由が2種類に集中した:

```
45件: 知らない命令 12（+0x20）
36件: まだ入れていない置き場所 3 を指している（+0x12c）
```

**命令12。** 実行ファイルの表（0x80010308）は最初から**15命令ぶん**あって、
コードも `if(命令-1 >= 15) 打ち切り` と書いてある。こちらが11までしか
知らなかっただけだった。しかも位置が `+0x20` ＝ #68 なら `命令9（四角）` が
来る場所。**命令12も面を描く命令**とみて間違いない。

（2つ目の「置き場所3」は、命令12 を読み飛ばせずに面の位置がずれた結果。
つまり原因は1つ。）

### 大きさは引き算で出る

面の領域は「枚数 × 1枚の大きさ」の合計にぴったり一致する。
命令10と11を割り出したときと同じ手が使える:

```
面の領域 = 20×(命令8の枚数) + 24×(命令9) + 12×(命令10) + 16×(命令11)
         + ?×(命令12の枚数)
                                     ↓
?  =（面の領域 − 分かっている合計）÷ 命令12の枚数
```

割り切れなければその読み方が間違い、という検算にもなる。

### 並べ方も数通りしかない

大きさが決まれば、中身の並べ方は限られる:

```
20B → 頂点3個(12B) ＋ 面に法線1つ(8B)
24B → 頂点3個(12B) ＋ 頂点ごとの法線番号(12B)
      または 頂点4個(16B) ＋ 面に法線1つ(8B)
```

候補を順に試して、**面の領域をぴったり使い切るもの**を採る。
使い切らなければ不採用なので、当てずっぽうが通ることはない。

試験では、命令12 を三角＋法線（20B）として組み立てた部品を用意し、
引き算で 20B と出ること・並べ方が「頂点3個＋法線」と決まること・
その読み方で面が正しく出ることを確かめた。

解析結果には割り出した結果が出る:

```
    割り出した: 命令12 は 1枚 20バイト（頂点3個・法線あり）　37部品で一致
```

## v3.26.0 — 分かったものを次に持ち越す（雪だるま式）

引き算は効いた。31部品で一致して、**命令13 は 1枚 24バイト（頂点3個）**と出た。
#143 #123 #142 #127 #130 #124 #126 #129 #145 #146 が人の形に立ち上がった。

ただし解けたのは**命令13 だけ**。引き算は「未知が1つのとき」しか使えないので、
命令12 と命令13 の両方を使っている部品は手つかずだった。

```
面の領域 = 分かっている合計 + ?×(命令12の枚数) + ?×(命令13の枚数)
                                  ↑ 未知が2つあると解けない
```

### 解いたものを覚えれば、次が解ける

命令13 を24バイトと覚えてしまえば、命令12 だけが未知になり、また引き算が使える。
**1つ解けるたびに分かっているものが増え、次が解ける。**

- `T1_LEARNED` に解けた命令を覚える
- `t1SolveSize` は覚えた命令も「分かっているもの」として扱う
- 部品を「進まなくなるまで」繰り返し試す（最大4周）
- 覚えた内容はファイルをまたいで残るので、あとのモデルほど有利になる

### 効いていることを試験で示す

「たまたま動いた」では困るので、雪だるま式が本当に効いていることを確かめる:

```
部品A: 命令13 だけ          → 引き算で 24B と分かる
部品B: 命令13 と 命令12     → A で覚えたおかげで 12 も 16B と分かる
覚えを消してから B だけ試す → 未知が2つなので解けない（＝覚えが効いていた証拠）
```

最後の1行が大事で、これが無いと「B は最初から解けていた」可能性を否定できない。

## v3.27.0 — 頂点を「入れる」命令を、まだ見落としていた

命令12・13 の大きさが分かっても、36体は **「まだ入れていない置き場所を指している」**
で止まったままだった。面の大きさの話ではなかった、ということ。

### 何が起きているのか

面の頂点番号は、本当の頂点番号ではなく「置き場所（スクラッチパッドの枠）」を指している。
その枠に頂点を入れるのが 命令1 と 命令2 だと思っていた。ところが面が
**まだ誰も入れていない枠を指してくる**。つまり、頂点を入れる命令を見落としている。

命令の形をよく見ると、見落としの候補がはっきりしている:

```
命令1  12B  (1, 置き場所, 個数)
命令7  12B  (7, ?, ?)          ← 命令1 と同じ形
命令2  16B  (2, 置き場所, 個数, 個数2)
命令4  16B  (4, ?, ?, ?)       ← 命令2 と同じ形
```

12バイトの命令が2つ、16バイトの命令が2つ、きれいに対になっている。
命令7 と 命令4 も頂点を入れる命令なのではないか。

### 決め打ちせず、当たりを探して覚える

命令12・13 のときと同じやり方にした。考えられる読み違いを全部並べて、
**「読んだ頂点数＝ファイルの頂点数」かつ「面のバイト数がぴったり」**
になるものを探す。当たったら覚えて、次の部品では先に試す。

| 読み方 | 中身 |
|---|---|
| 命令4も頂点を入れる | 16バイトの対 |
| 命令7も頂点を入れる | 12バイトの対 |
| 語1が個数・語2が置き場所 | 引数の順番が逆 |
| 置き場所は4で割る | 語1がバイト位置だった場合 |
| 個数は+1 | 「n個」ではなく「n+1個」だった場合 |

全部で31通り。合否の条件が厳しい（頂点数と面のバイト数の両方がぴったり）ので、
たまたま通ることはまずない。既定の読み方は必ず先に試すので、
いま読めているものが壊れることもない。

### 読めなかった部品を「数えるだけ」で調べる

総当たりでも当たらなかったときのために、**面のバイトを一切消費しない調べ方**を足した。
途中で読み違えても数は狂わないので、読めない部品でも次の2つは必ず分かる:

- 面のバイト数の勘定が合うか（＝1枚の大きさの表が正しいか）
- 頂点を入れる命令の個数の合計が、ファイルの頂点数と合うか

後者が足りなければ「入れる命令をまだ見落としている」、
合っているのに読めないなら「入れる場所の数え方が違う」と、原因が二択まで絞れる。
結果は解析の報告にそのまま出る。

```
#45 頂点 51 ／ 入れる合計 40（＋4と7で 11） [命令1:32 命令2:8 命令7:11] ／
    面 768B 勘定 ぴったり ／ 命令 1×3 2×1 5×2 7×2 10×20 11×33
```

この1行が出れば、次に何を直せばいいかは読めば分かる。

## v3.28.0 — 残り3体。原因は2つとも報告に出ていた

v3.27.0 の総当たりで **命令7 も頂点を入れる命令**だと分かり、
読めたキャラクターは **4体 → 37体** になった。残りは #45 #142 #143 の3体だけ。
そして新しく足した「数え上げ」に、詰まっている理由がそのまま出ていた。

```
#45  頂点 749 ／ 入れる合計 749 ／ 面 14744B 勘定 未知の命令 12,13
#143 頂点  31 ／ 入れる合計  31 ／ 面   344B 勘定 未知の命令 14,15
```

どちらも「入れる合計＝頂点数」。**頂点の入れ方はもう合っている。**
残っているのは面の1枚の大きさだけ、と二択のうち片方に絞れた。

### #45 は、順番の問題だった

命令12・13 の大きさは #142 #143 あたりで割り出して覚えている。
ところが **#45 のほうが先に処理される**（sector 5737 対 9274）ので、
#45 を見たときにはまだ覚えていなかった。ふるい分けは1周しかしていなかった。

覚えたことは後から効くので、**読めなかったファイルだけもう一度かける**ようにした
（最大3周。進まなくなったら止める）。

### #142 #143 は、未知が2つ

命令14 と 命令15 が同じ部品に出てくる。引き算は未知が1つのときしか使えない。

```
面の領域 = 分かっている合計 + ?×(命令14の枚数) + ?×(命令15の枚数)
```

そこで **片方の大きさを決め打ちして、もう片方を引き算で出す**、を総当たりにした。
1枚の大きさは 8〜64 の4の倍数しかないので、組は高々十数通り。
そのうえ「読んだ頂点数＝ファイルの頂点数」かつ「面のバイト数がぴったり」を
通るものはさらに少ない。**通る組が1つに絞れたときだけ覚える**
（絞れないのに覚えると、あとの部品を巻き添えにする）。

### 実行ファイルからも裏を取れるようにした

総当たりで絞れなかったときのために、実行ファイル側の手がかりも出すようにした。
命令の表 `0x80010308` の処理はどれも

```
lw   $a0, 0($s0)          ← 枚数
lw   $v0, [0x800CC870]
addu $v0, $a0, $v0        ← 数を足して
sw   $v0, [0x800CC870]
jal  0x8001B8B8           ← 描く関数を呼ぶ  ← ここが命令ごとに違う
```

という同じ形をしていて、**違うのは `jal` の飛び先だけ**。
飛び先が同じ命令どうしは1枚の大きさも同じはず、ということになる。
報告に「命令 10,14 → 0x8001B8D0」のようにまとめて出し、
その関数の中身も10命令ぶん載せるようにした。総当たりの答え合わせに使える。

## v3.29.0 — 報告を短くする（やりとりの量を減らす）

解析の報告が毎回 4万字近くになっていた。中身のほとんどは
**実行ファイルの逆アセンブル・ファイルの一覧・生のバイト列**で、
どれも前回と同じ内容。やりとりのたびにこれを貼るのは、貼る側にも読む側にも重い。

### 既定を「短い報告」にした

長い部分は「実行ファイルや生の中身まで出す」を入れたときだけ出すようにした。
残るのは **前回と変わりうるもの** だけ:

- キャラクターのモデルが何体読めたか / 読めない理由
- 読めなかった部品の数え上げ
- 割り出した命令の大きさ・覚えた読み方

試験用の小さなディスクで 14,268字 → 1,941字（約 1/7）。
実物のディスクでは 4万字 → 3千字ほどになる。

### 「作者に送るまとめ」ボタン

報告からさらに絞って、**変わったところだけ**を集めたボタンを足した。
実物で 1〜2千字。これだけ貼れば、次に何を直せばいいかは決まる。

長い報告が要るときは、こちらから「詳しく出すほうを入れて貼ってください」と頼む。
ふだんは短いほうで足りる。

## v3.30.0 — 骨をかける道を、落とすだけで済むようにした

40体ぜんぶ「読めた」のに、人の形にならない。理由は `骨ごとの置き場所` に出ていた。

```
[ 4] 12枚  範囲 68..298   -75..73  -70..76   ┐
[ 5]  9枚  範囲 -34..298  -72..71  -64..74   │ 右腕
[ 6] 24枚  範囲 -4..395   -74..62  -57..70   │
[10] 12枚  範囲 68..298   -76..73  -76..70   ┐
[11]  9枚  範囲 -34..298  -72..71  -74..64   │ 左腕（Z が反転しただけ）
[12] 24枚  範囲 -4..395   -74..62  -70..57   │
```

4↔10、5↔11、6↔12 と **Z を反転しただけの対**が並ぶ＝左右の手足。
どれも **長さ 300〜400・太さ 130 前後の細長い棒**で、**ほぼ原点から生えている**。

つまり頂点は **その骨のローカル座標**で入っている。骨の行列をかけないと、
腕も脚も背の高さ方向に重なって胴に畳み込まれる。あの塊はそれだった。
「読めた」は手段の話で、目的の話ではなかった。

### 行列はファイルの中に無い

`0x800CBE8C/90/94` はどのコードからも進められるだけで、
初期化しているコードが実行ファイルに無い。実行中に作られる値で、
モデルファイル側にも行列の並びは無い（#68 の全区画は頂点・法線・面・色で説明がつく）。
取るならメモリから。

### 番地に頼らずメモリから探す

`0x800CBE90` は描いている最中に進むカーソルなので、保存した瞬間の値はあてにならない。
番地を当てにせず、RAM 全体から2つの形を探す:

| 形 | 中身 |
|---|---|
| 行列の並び | 32バイトの行列がずらりと続いている場所 |
| 位置の表 | 行列を指す位置がずらりと並んでいる場所（表A はこちら） |

回転行列は「3本の行の長さがそろっていて互いに直角」というきつい条件を満たすので、
でたらめなバイト列が紛れ込むことはまずない。
見つかった中から **モデルの区切りの数に足りて、いちばん近いもの** を選ぶ。

### 落とせばそのまま当たる

セーブステートを選ぶと、骨さがし → 表の選択 → モデルの組み立て直しまで自動で進む。
区切りごとに行列をかけて頂点を運ぶので、うまくいけばその場で手足が伸びる。

```
DuckStation → 設定 → 詳細 → セーブステートの圧縮 → Deflate か なし
→ キャラクターが立っている画面でステートを保存
→ その .sav をビューアに落とす
```

作りもののRAMで、表を見つけること・移動量まで取り出せること・
区切りの数に合う表を選ぶこと・行列をかけると区切りが動くことを試験にしてある。

## v3.31.0 — 同じ形のものをまとめる（40体は何人ぶんなのか）

一覧を見ていると、色も模様も違うのに **形がまったく同じ**ものがいくつもある。

```
#29〜#37   三角形 1118 ×9体   sector が 13 ずつ等間隔
#68 #60    三角形 1218 ×2体
#27 #28 #151 三角形 1235 ×3体
#148 #149  三角形 1424 ×2体
```

三角形の数も部品の数も一致するなら、同じモデルの **色違い・衣装違い**。
40体を畳むと実際のキャラ数に近づくはずで、これは骨が無くても出せる。

一覧に **組A・組B…** の名札を付け、「作者に送るまとめ」にも
どの番号が同じ組なのかを出すようにした。名前を当てるときの土台になる。
